# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog.

https://keepachangelog.com/

This project follows Semantic Versioning.

https://semver.org/

---

## [Unreleased]

### Added

- ADAPT-014 (issue #66): the Relationship *Definition* editor — a screen for Alberto's stated authoring need ("adding new relation types"). Since the CORE-004 persistence fast-follow, Definitions have been real editable `StorageProvider` state (migration 3), reachable only via the `game.modules.get('archivexus').api` console escape hatch; this replaces that with a UI. `src/adapters/foundry/relationship-definition-editor-window.ts` (new): a singleton `ApplicationV2` (deferred-factory-class, raw `_renderHTML` string / `_replaceHTML` contract, pure content builders — same shape family as `relationship-list-window.ts` / `relationship-authoring-window.ts`), a list panel + an add/edit form side by side. **List** — every Definition from `storage.listRelationshipDefinitions()`, `sortDefinitions` groups by `traversalCategory` (taxonomy order) then alphabetically by `name`; each row shows the forward→inverse (or ↔ for symmetric) label, category · cardinality, and Edit / Delete. **Add** — `slugifyDefinitionId(name, existingIds)` mints the id (numeric suffix on collision, `relationship-type` fallback), `version` starts at 1. **Edit** — upsert by the existing id, `version` bumped +1 in place (no historical rows kept — that stays `03_DOMAIN_MODEL.md`'s open migration question). **Delete** — a `DialogV2.confirm` (same shape `relationship-list-window.ts` researched), no cascade (existing Relationships whose `definitionId` pointed at it render uncategorized until re-pointed, per ADR-0007 point 8). All correctness lives in `createRelationshipDefinition` — `parseDefinitionForm` maps the form's `[name=…]` values to a `CreateRelationshipDefinitionInput` (comma-separated `validation` allow-lists parsed, blanks dropped; a symmetric type's `inverse` forced to equal its `name`), `validateDefinitionForm` runs the factory and turns a thrown `InvalidRelationshipDefinitionError` into an inline `data-role="form-error"` message shown *without* a re-render so typed input survives. Launched from a new GM-only **"Relationship types"** button on the Codex sidebar toolbar — `buildNavigatorShellHTML`'s `withGuidance` option generalized to `isGM`, now gating that button alongside the VIEW-001g guidance affordances. `foundry-globals.d.ts` unchanged (`DialogV2` was already ambient). `index.ts` exports the new surfaces. **Out of scope** (per issue #66): Definition version migration, bulk import/export. 27 new unit tests (464 total, up from 437); `tsc --noEmit` / `eslint` / `vitest run` / `build:foundry-module` all clean (`archivexus.js` ~74KB → ~86KB). **Judgment calls flagged:** the id-from-name slug (vs. `randomID`), and editing bumping `version`. **Not yet live-verified:** the `ApplicationV2` render + `actions`/`data-action` wiring, `DialogV2.confirm` for delete, and the `[name=…]` form-value reads — same discipline as every prior Foundry-glue file in this lineage. Unblocks VIEW-001e's cluster category labels.
- VIEW-001g (issue #64): first-run guidance — a visible pointer at the per-sheet setup entry points ("Archivexus Node Type", "New Relationship…", "Relationships…"), which aren't broken but are buried two clicks deep in each sheet's "⋯ Toggle Controls" header dropdown. `src/adapters/foundry/first-run-guidance.ts` (new): `GUIDANCE_STEPS` (the three steps — type Nodes → connect them → open the graph — as one source of truth shared by the dialog and the panel), `buildWelcomeDialogContent(counts)` and `buildGuidancePanelHTML(state)` (pure markup), `registerOnboardingSetting(settings)` (registers a world-scoped, `config: false` boolean `onboardingDismissed` flag — first `game.settings` use in the codebase; "the world has been set up" is a world fact, and the module may be shared/published), and `maybeShowWelcomeDialog(deps)` (GM-only, once-per-world; shows a `DialogV2.wait` welcome dialog with "Show me the Codex" / "Dismiss" — **any** close marks it dismissed so it never nags; only the primary choice also switches the sidebar to the Codex tab). `codex-sidebar-tab.ts` renders the inline guidance panel (GM-only) into a new `data-role="guidance-mount"` on every load: expanded while `storage.listRelationships()` is empty (the navigator's proper empty state), collapsed to a "▸ Getting started" toggle once ≥1 Relationship exists, re-expandable via the toggle or a new "Getting started" toolbar button. `module-entry.ts` registers the setting at `init` and calls `maybeShowWelcomeDialog` at `ready` after the backfill, with a best-effort `activateCodexSidebarTab` (`ui.sidebar.changeTab` → `activateTab` fallback for the v13 rename). `README.md` gains a "Getting started (in Foundry)" section. `foundry-globals.d.ts` + `eslint.config.js` unchanged for globals (`game.settings`/`ui.sidebar` added to the ambient `game`/`ui` shapes only). **Out of scope** (unchanged from the ticket): coach-marks / a tour library, progress tracking beyond the one flag, auto-typing Nodes. 13 new unit tests (436 total, up from 423); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean (`archivexus.js` ~74KB). Not yet live-verified: the real `DialogV2.wait` render, `game.settings` round-trip on a running world, and the `ui.sidebar` tab switch.
- VIEW-001c (issue #63): the Codex sidebar tab becomes a **node navigator**, per `decisions/ADR-0014-graph-view-sidebar-tab.md`'s Amendment A2. VIEW-001a's in-tab Cytoscape rendering is removed — the ~300px sidebar was too cramped for a canvas, and the graph now lives entirely in the VIEW-001b popout. The tab is a search box + a `node.type`-grouped list of every Node: clicking a row opens/re-roots the popout on that Node (`openGraphPopout(getStorage, log, { rootNodeId })`), and an "Open graph ⧉" toolbar button opens it on the whole graph. `src/adapters/foundry/node-navigator.ts` (new, pure, fully unit-tested): `groupNodesByType(nodes)` (group order per A2 — place-like `City`/`Kingdom` first, then `Character`, then `Organization`, then the rest of `KNOWN_NODE_TYPES` alphabetically, then GM-invented types alphabetically; alpha by title within a group; empty groups omitted) and `filterNodesByQuery(nodes, query)` (case-insensitive title substring; empty query matches all). `codex-sidebar-tab.ts` rewritten: pure builders `buildNavigatorShellHTML` / `buildNavigatorGroupsHTML` / `buildNavigatorStateHTML` (loading / empty / error), and the `AbstractSidebarTab` subclass keeps the deferred-factory-class + raw `_renderHTML`/`_replaceHTML` pattern and the `Hooks.on('archivexus.ready', …)` re-fetch (storage is created after the sidebar first renders). Search filters client-side by toggling row/group visibility off a lowercased `data-title` — no re-render, so the input keeps focus. `filterNodesForViewer` (ADR-0003) still runs on every load path; the "· N hidden" count stays GM-only. No Cytoscape import here any more (`cytoscape-loader.ts` / `graph-view-elements.ts` are still used by the popout). `index.ts` swaps `buildCodexContentHTML` for the new builders and re-exports `node-navigator`. Node-type graph colouring, once listed as a VIEW-001c fold-in, moves to a popout follow-up (ADAPT-013) since the sidebar no longer draws a graph. +11 unit tests (423 total, up from 412 — `node-navigator.test.ts` plus the navigator builders, net of the replaced `buildCodexContentHTML` tests); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean (`archivexus.js` ~67KB; Cytoscape still its own code-split chunk, now reached only from the popout). **Live-verified on v14.367 (GM):** navigator renders (Character 41 / Lore 61, "102 nodes"), correct group order, live search filters and keeps the input focused, a row click opens the popout re-rooted on that Node, "Whole graph" → 102 nodes, no console errors. This deploy was also the first live run of SQLite **migration 2→3** + `bootstrapRelationshipDefinitions` on a real OPFS DB ("Seeded 17 default Relationship Definitions." clean) — RelationshipDefinition persistence had not been live-verified before either.
- Relationship Definition persistence (CORE-004's deferred fast-follow): Definitions are real, editable `StorageProvider` state now, not the hardcoded `SEEDED_RELATIONSHIP_DEFINITIONS` array ADAPT-007 carried. `StorageProvider` gains `saveRelationshipDefinition`/`getRelationshipDefinition`/`deleteRelationshipDefinition`/`listRelationshipDefinitions`, implemented in `SqliteStorageProvider` against a new SQLite **migration 3** `relationship_definitions` table (every field a real column with `CHECK` constraints on `cardinality`/`symmetry`/`traversal_category`, except the optional `validation` allow-list which is a JSON column; PK `id` alone, `version` a plain column; deliberately **no** FK from `relationships.definition_id` — same no-cascade reasoning as every other cross-table reference, ADR-0007 point 8). `row-mapping.ts` gains `relationshipDefinitionToRow`/`rowToRelationshipDefinition`; `worker/protocol.ts` + `worker-storage-provider.ts` gain the four RPC calls (`rpc-dispatcher.ts` is method-generic — no change). `src/core/domain/relationship-definitions-default.ts` (new) holds `DEFAULT_RELATIONSHIP_DEFINITIONS` — a starter vocabulary of ~17 definitions covering all 8 `traversalCategory` values (`resides-in`, `located-in`, `member-of`, `ally-of`, `rival-of`, `parent-of`, `sibling-of`, `spouse-of`, `enemy-of`, `at-war-with`, `rules`, `serves`, `participated-in`, `founded`, `owns`, `knows`, `related-to`) — in Core, because relationship vocabulary is domain content, not a platform concern. `src/adapters/foundry/relationship-definitions-bootstrap.ts` (new) seeds it into an **empty** store on `ready` (idempotent by "is the store empty" — a customized set is never re-clobbered); `module-entry.ts` calls it and exposes `listRelationshipDefinitions`/`saveRelationshipDefinition` on the module `api` as a console escape hatch. The Relationship-authoring window (`relationship-authoring-window.ts`) and the graph popout's Inspector (`graph-popout-window.ts` → `node-connections.ts`) now read Definitions from storage, falling back to the Core default list only if the store is somehow empty. `src/adapters/foundry/relationship-definitions-seed.ts` (+ its test) **deleted**. The exact default list and its per-definition cardinality/validation choices are flagged judgment calls, to be adjusted in place. **Deliberately still open** (not this ticket): a Foundry UI to author/edit Definitions (data layer + console escape hatch only), and Definition *version migration* (`03_DOMAIN_MODEL.md`'s Definitions Open Question — an existing world is never re-seeded). 17 new unit tests (412 total, up from 400 — net of −5 for the deleted seed test); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean. Not yet live-verified: a real OPFS `PRAGMA user_version` 2 → 3 upgrade + the bootstrap on a running Foundry client.
- VIEW-001b (issue #57): the graph popout window + Inspector panel, implementing `decisions/ADR-0014-graph-view-sidebar-tab.md`'s Amendment (A1/A3/A3a/A4/A5). `src/adapters/foundry/graph-popout-window.ts` (new): a standalone, resizable, **singleton** `ApplicationV2` window (deferred-factory-class + raw `_renderHTML`/`_replaceHTML`, same pattern as `relationship-list-window.ts`) rendering the campaign graph with Cytoscape.js — launched from VIEW-001a's sidebar tab via a new "Open graph ⧉" button (`openGraphPopout`, which focuses/re-roots the one open window rather than spawning a duplicate). Toolbar: "Whole graph", a `(Direct)(Everything)(Curated — disabled, VIEW-001f)` preset segmented control, a layout picker (`auto`/force/grid/circle/concentric/tree), and a GM-only "Preview as player (approximate)" toggle that re-filters with `isGM: false` and shows a banner. Gestures per Amendment A3: single-tap = select (highlights the node + populates the Inspector), double-tap = open the Foundry sheet (`foundry.utils.fromUuid` + `sheet.render(true)`), right-click = a small context menu (Open sheet / Re-root here / Everything connected here) hosted on a dismiss-on-click overlay. The **Inspector panel** (right dock, A3a) shows a selected Node's title/type, its **Attached** Blocks as clickable links (`fromUuid(block.uuid)` — `Block.uuid`/`title` already on the Node, no new Core/Storage), and its **Connections** grouped by `traversalCategory`. `src/adapters/foundry/node-connections.ts` (new, pure, fully unit-tested) implements ADR-0012 points 4–7's connection-list logic — one row per (Relationship, connected Node) pair, direction-correct label (`definition.name`/`inverse` by which end the root is), grouped by the closed 8-value taxonomy (title-cased) with an "Other" group last for unresolved Definitions, sorted degree-descending / alpha-tiebreak within each group — reusable by the still-unbuilt ADAPT-011 standalone Connections panel (Rule 4). Uses the temporary `SEEDED_RELATIONSHIP_DEFINITIONS` for category resolution until `RelationshipDefinition` persistence lands (same known gap as ADAPT-007). `src/adapters/foundry/cytoscape-loader.ts` (new) extracts VIEW-001a's guarded lazy Cytoscape loader (the `Array.prototype.equals` workaround) + the shared structural types / style / `layoutFor` so both the sidebar tab and the popout reuse it; `src/adapters/foundry/foundry-viewer.ts` (new) holds the shared `isViewerGM()` glue. `index.ts` exports the new surfaces. Non-GM viewers (and a GM previewing-as-player) only see non-`hidden` Nodes (ADR-0003) on every render path. 20 new unit tests (400 total, up from 380); `tsc --noEmit`/`eslint`/`vitest run` all clean, `build:foundry-module` clean (`archivexus.js` ~47KB → ~63KB, Cytoscape still its own code-split chunk). **Live-verified on v14.367 (GM):** popout opens, singleton, whole-graph render, layout picker, single-tap → highlight + Inspector, double-tap → Actor sheet, right-click → overlay context menu + "Re-root here", "Preview as player" → 3 nodes + banner, no console errors. Not exercised (nothing to test against): the Inspector connections list + "Everything" preset (0 authored Relationships), and the context menu's real screen positioning. "Everything connected" collapsed-cluster grouping is VIEW-001e; the sidebar-tab-to-navigator conversion is VIEW-001c; favourites are VIEW-001d. Not merged to `dev`, not pushed.
- VIEW-001a (issue #56): the Codex — a first-level Foundry sidebar tab rendering the campaign's whole Knowledge-Element graph, per `decisions/ADR-0014-graph-view-sidebar-tab.md`. Adds `cytoscape ^3.34.3` to `dependencies` (ADR-0014 point 3's accepted new runtime dependency — no native Foundry graph renderer exists; ships its own TypeScript types). `src/adapters/foundry/graph-view-elements.ts` (new, pure, fully unit-tested): `buildGraphViewElements(nodes, relationships)` transforms domain objects into Cytoscape `{ group, data }` elements — an edge is emitted only when both endpoints are present, so a Relationship with a dangling endpoint is dropped the same way `resolveTraversal` drops it (ADR-0007 point 8), never a placeholder node; `buildGraphViewElementsFromTraversal(TraversalResult)` re-adds the (by-design-excluded) query root and feeds the same transform. `src/adapters/foundry/codex-sidebar-tab.ts` (new): `registerCodexSidebarTab(config, getStorage, log)` writes both halves of Foundry's native sidebar-tab registration — the descriptor at `CONFIG.ui.sidebar.TABS.codex` (`{ tooltip, icon }`, the shape confirmed against v14's own `settings` tab) and the tab class at `CONFIG.ui.codex`. `getCodexSidebarTabClass` builds the `foundry.applications.sidebar.AbstractSidebarTab` subclass (deferred-class pattern like `relationship-list-window.ts` — no Handlebars mixin, so the raw `_renderHTML`/`_replaceHTML` contract): whole-graph overview on render, single-tap a Node → its "Direct only" 1-hop neighbourhood via `resolveTraversal` (CORE-005, unchanged), double-tap → open its real Foundry sheet (`foundry.utils.fromUuid` + `sheet.render(true)`), a "Whole graph" reset action, and a one-time `<style>` injection (`ensureCodexStyles`) so the Foundry build stays a single `archivexus.js` with no separate CSS asset. Wired into `module-entry.ts` at `init`; `foundry-globals.d.ts` and `eslint.config.js` gained `CONFIG`, `foundry.applications.sidebar` and `globalThis`. 16 new unit tests (375 total, up from 359); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean. **First live deploy (v14.367) crashed at bundle eval and the root cause was fixed:** Foundry v13+ locks `Array.prototype.equals` (and `deepFlatten`/`filterJoin`/`findSplice`/`partition`) non-writable + non-configurable; Cytoscape's collection prototype is `Object.create(Array.prototype)` and `Object.assign`s its own `equals` onto it during module init → an uncatchable `TypeError` that killed the whole module. Cytoscape is therefore **not** a static `import` — it's loaded via a guarded dynamic `import()` (`ensureCytoscape`) that swaps in a defensive `Object.assign` (falls back to `defineProperty` where `[[Set]]` would throw) for the duration of Cytoscape's evaluation, then restores it. This also code-splits Cytoscape into its own chunk (`cytoscape.esm-*.js`, ~639KB / 168KB gzip), keeping `archivexus.js` at ~46KB and its load path synchronous-safe. Two further live fixes: `module-entry.ts` fires `Hooks.callAll('archivexus.ready', storage)` after backfill so the tab (which renders before Foundry's `ready`) re-pulls the graph once STORE-003's storage exists; and the graph is held in `#pendingElements` and only laid out while the tab is the visible one (Cytoscape lays out against container pixels — a hidden 0×0 tab piled all 102 nodes at the origin), flushed by `_onActivate`. **Verified end-to-end on Foundry v14.367:** the Codex auto-loads all 102 real campaign nodes in a grid on open; single-tap a node → its "Direct only" view (`resolveTraversal`); double-tap → opens its real Foundry sheet (`fromUuid` → `NPCActorSheet`, confirmed); "Whole graph" reset works; no console errors. Known cosmetic issue, non-blocking: grid node labels overlap in the ~300px sidebar (leave for VIEW-001b or a quick follow-up). Post-review addendum (same branch): (a) **visibility fix** — the Codex was rendering `storage.listNodes()` unfiltered, so a player saw a GM-only Node; new pure `filterNodesForViewer(nodes, { isGM })` in `graph-view-elements.ts` (ADR-0003 — `hidden` = GM-only) is applied to every render path (`isViewerGM()` reads `game.user.isGM`, safe-defaults to `false`), and the whole-graph hint gains a "· N hidden" suffix for GMs; conservative — filters on `Node.visibility` from default ownership, so a `hidden` Node a specific player was granted access to stays hidden (a per-user resolution belongs with a real `View` scope). (b) **the "N hidden" hint suffix is GM-only** (a player mustn't learn the size of content they can't see). (c) **VIEW-001b redesign decided** — a ux-ui-designer pass, formalized as `decisions/ADR-0014-graph-view-sidebar-tab.md`'s Amendment (2026-09-08, A1–A8): the Cytoscape canvas moves into a standalone singleton `ApplicationV2` popout window; the sidebar tab becomes a node navigator (search + `node.type`-grouped list, no graph); favourites = a pinned navigator group backed by `game.user` flags (per-user, explicitly not Core state / a `View` / in the portable snapshot); gesture map single-tap = select + a new Inspector panel (renders a Node's attached Blocks as clickable links + connections list reusing ADR-0012's grouping — Alberto confirmed his "double-tap → related journals/actors" meant *attached content*, not a 2-hop expansion), double-tap = open sheet, right-click = context menu; no player-facing "something hidden" placeholder + a GM-only "preview as player" toggle; CORE-006 stands unchanged. Revised ticket split VIEW-001b (popout + Inspector) → VIEW-001c (navigator) → VIEW-001d (favourites) → VIEW-001e (clusters) → VIEW-001f (curated + saved layout). Architect follow-ups (A8): favourites-as-user-flags confirm, per-user visibility resolution scope, whole-graph-as-View (conditional), `RelationshipDefinition` persistence as a VIEW-001e pre-req. (d) VIEW-001a's gestures **trimmed to a clean subset** of the Amendment's A3 in the same commit: single-tap = "Direct only", double-tap = open sheet; the interim "double-tap = everything connected" and the `cxttap` right-click binding removed (both move to the VIEW-001b popout); `_focusNode` lost its `preset` parameter. 380 unit tests (359 → +21: element transform + `filterNodesForViewer` + `collectTraversalNodes`). `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean. Not merged to `dev`, not pushed.
- CORE-006 (issue #55): gives `View` its first real Core shape and adds `StorageProvider` View CRUD, per `decisions/ADR-0014-graph-view-sidebar-tab.md` points 6-7. `src/core/domain/view.ts` (new): `View extends KnowledgeElement` (`kind: 'view'`, `format: 'graph'` — `VIEW_FORMATS`, a plain one-value closed union, not a `ViewDefinition` registry, per ADR-0014 point 6b), `spec: GraphViewSpec` (a discriminated union — `direct-only`/`everything-connected` carry only `preset` + `rootNodeId`; `curated-by-me` adds a trimmed/de-duplicated `relationshipIds` list and an optional `layout` of finite `{ x, y }` node positions). `createView`/`isView`/`isGraphViewSpec` follow `createNode`/`createRelationship`'s validating-factory style (composition, not subclassing — Rule 5). `GRAPH_VIEW_PRESETS` is a by-convention copy of CORE-005's `TRAVERSAL_PRESETS`, kept out of a `core/domain` → `core/query` dependency (same non-dependency `relationship-definition.ts` keeps), with a sync comment on both sides. `StorageProvider` gains `saveView`/`getView`/`deleteView`/`listViews`; `SqliteStorageProvider` implements them against a new SQLite **migration 2** `views` table — JSON-encoded `spec` TEXT column, `CHECK (format IN ('graph'))`, and deliberately **no** foreign key to `nodes`/`relationships` (same "no cascade, no delete-blocking" reasoning as migration 1's `relationships` table: a View referencing a since-deleted Node just resolves fewer results next open — ADR-0014 point 7 / ADR-0007 point 8). `row-mapping.ts` gains `viewToRow`/`rowToView`; `worker/protocol.ts`'s method union and `worker-storage-provider.ts`'s proxy methods gain the four View calls (`rpc-dispatcher.ts` is method-generic — no change). No Foundry/Adapter code touched (the sidebar tab and Cytoscape rendering are VIEW-001a/VIEW-001b). `to-portable-snapshot.ts` still does not gather Views — its own header already frames that as a separate follow-up; the export DTO keeps its placeholder `views: []`. 35 new unit tests (359 total, up from 324); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean in-sandbox. Judgment call flagged: `createView` rejects (not silently strips) a derived-preset `spec` carrying `curated-by-me`-only fields, mirroring `relationship-definition.ts`'s cross-field validation. Not merged to `dev`, not pushed — a real OPFS-backed `PRAGMA user_version` 1 → 2 upgrade inside a running Foundry client still needs live verification, same discipline as STORE-003.
- ADAPT-012 (issue #53): implements `decisions/ADR-0013-relationship-deletion-ui.md` — a GM can now see and delete a Node's Relationships. `src/adapters/foundry/relationship-list-window.ts` (new file) adds a third `getHeaderControls*` button, "Relationships…" (`fa-solid fa-list`), on both Actor and JournalEntryPage sheets, opening a dedicated `ApplicationV2` list window pre-scoped to the clicked document's own Node id (reusing `relationship-authoring-window.ts`'s existing `resolveDroppedDocumentNode` call and exported structural types — no drop step needed, only one entity involved). Lists every `storage.getRelationshipsForNode(nodeId)` result, rendering each row's own already-persisted `title` verbatim (Rule 4 — no new endpoint-resolution logic); empty state is "No relationships yet." Each row's Delete button uses the `ApplicationV2` `actions` map's per-element `(event, target)` form (researched against Foundry's official API docs — `ApplicationClickAction`'s documented `(event: PointerEvent, target: HTMLElement) => void | Promise<void>` shape, `this`-bound to the Application instance; the row id is resolved via `target.closest('[data-relationship-id]')` off the clicked button's enclosing `<li>`), a genuinely new-to-this-codebase surface since `relationship-authoring-window.ts`'s own single Save button never needed to disambiguate multiple elements sharing one `data-action` value. Clicking Delete shows `foundry.applications.api.DialogV2.confirm(...)` with that row's own title text (also previously unexercised — only `.prompt` had been used before, ADR-0009's Amendment — researched the same way against the official docs: resolves `true`/`false`/`null`, treated here as "only an explicit `true` proceeds"). On confirm, `storage.deleteRelationship(id)` is called and just that row is removed from the DOM — the window stays open, supporting the ADR's named "batch cleanup in one sitting" use case. Needed zero `StorageProvider` change (`deleteRelationship`/`getRelationshipsForNode` both already existed end-to-end — confirmed via grep before writing anything, not just trusted from the ADR's own claim). Wired into `module-entry.ts` as a third registration; `header-controls-coexistence.test.ts` extended from n=2 to n=3 on both `getHeaderControlsJournalEntryPageSheet` and (newly) `getHeaderControlsActorSheetV2`, asserting all three independent listeners fire and all three push into one shared `controls` array — a real, named ADR-0013 requirement. 23 new unit tests in `relationship-list-window.test.ts` (324 total, up from 300); `tsc --noEmit`/`eslint`/`vitest run` all clean, no regressions. Judgment call flagged: an unresolvable clicked-document type means the window simply doesn't open, unlike the sibling authoring window's optional-prefill handling. Not merged to `dev`, not pushed — needs live Foundry verification of both newly-researched API surfaces and the third button's real on-sheet coexistence, same discipline as every prior `getHeaderControls*` addition in this lineage.
- ADAPT-009 (issue #49): implements `decisions/ADR-0011-journal-entry-page-node-attachment.md` (+ its DBA Amendment) — a GM can now attach a JournalEntryPage to an existing Node instead of it always becoming its own standalone Node. `src/core/domain/block.ts`'s `Block` gets its real `{ type, uuid, title? }` shape (replacing CORE-001's `{ id, type, data }` placeholder), plus a validating `createBlock`/`isBlock` factory matching `createNode`/`createRelationship`'s style. `src/core/domain/node.ts` gains `upsertBlockByUuid`/`removeBlockByUuid` — pure, Foundry-independent Node/Block manipulation, unit-tested for append/replace/remove/no-op-remove. `src/adapters/foundry/journal-entry-page-node-tag.ts` extends ADR-0009's `DialogV2` tagging control to JournalEntryPage sheets via `getHeaderControlsJournalEntryPageSheet` (researched against Foundry v14's official, documented class hierarchy and hook-inheritance behavior — not live-verified), adding a `<document-tags single>` field to attach to an existing Node alongside the existing type input; coexists with `relationship-authoring-window.ts`'s own listener on the same hook. `journal-entry-page-to-node.ts` gains a pure `resolvePageAttachment` reading `flags.archivexus.attachedToNodeId`; `mapJournalEntryPageToNode` itself is unchanged. `storage-sync.ts`'s `syncJournalEntryPage` now resolves attachment: deletes the page's standalone Node and upserts a `{ type: 'JournalEntryPage', uuid, title }` Block onto the target when it resolves; falls back to standalone when the target is dangling or the flag is unset; and runs an unconditional `listNodes()` cleanup scan on every call to remove stale Block references from any non-target Node (also correctly handling detach-to-standalone). 47 new unit tests (298 total); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean. ADR-0011 point 5's non-blocking `ui.notifications.warn` about Relationships orphaned by a retag was out of this ticket's explicit orchestration scope at the time — closed out by ADAPT-010 below. Not merged to `dev`, not pushed — needs live Foundry verification first, same discipline as every prior `getHeaderControls*` addition.
- ADAPT-010 (issue #50, reviewer follow-up on ADAPT-009): implements ADR-0011 point 5's non-blocking `ui.notifications.warn`. `storage-sync.ts`'s `syncJournalEntryPage` now calls `storage.getRelationshipsForNode(page.uuid)` before deleting a page's own standalone Node in the attach-resolves-to-a-real-target branch; if any Relationships exist, warns the GM by name/count (e.g. "Biografía had 2 Relationship(s); they're preserved but excluded from the graph until re-authored against Fausto Farcon directly") before the delete/attach proceeds regardless — warn, never block, per the ADR's own text. `foundry-globals.d.ts` gains a minimal `ui.notifications.warn` ambient declaration (`eslint.config.js`'s `FOUNDRY_ADAPTER_GLOBALS` kept in sync). New test case in `storage-sync.test.ts`'s ADR-0011 orchestration suite confirms the warning fires with the correct count and that the delete/attach still proceeds unaffected.
- Reviewer follow-up on ADAPT-009 (same branch): a new `header-controls-coexistence.test.ts` fills a real coverage gap — `journal-entry-page-node-tag.ts`'s and `relationship-authoring-window.ts`'s `getHeaderControlsJournalEntryPageSheet` registrations were previously only ever tested against each module's own isolated mock `Hooks`, never together. The new test wires both real registration functions against one shared mock `Hooks` (tracking an array of listeners per hook name, matching real Foundry semantics) and fires every registered listener for that hook name against one shared `controls` array. Confirms the coexistence claim already asserted in code comments and `docs/PROJECT.md` is correct: both `Hooks.on` calls register independently (2 listeners under the same hook name, not 1 replacing the other), and firing the hook once pushes both modules' entries ("Archivexus Node Type" and "New Relationship…") into the same `controls` array.
- CORE-005 (issue #42): the engine-agnostic Core Query API resolving ADR-0007's 3 fixed MVP traversal presets against a Node. `src/core/query/traversal.ts` exports `resolveTraversal`/`TRAVERSAL_PRESETS`/`EVERYTHING_CONNECTED_DEPTH` and the `TraversalQuery`/`TraversalResult` types, built on `StorageProvider.getRelationshipsForNode` (STORE-003's 1-hop primitive) with no Foundry-specific code and no View-layer concerns (rendering, preset-switching, or `RelationshipDefinition` persistence are all out of scope). "Direct only" is a single `getRelationshipsForNode` call; "Everything connected" composes a cycle-safe, visited-set-bounded BFS to a Core-level `EVERYTHING_CONNECTED_DEPTH = 2` constant, matching ADR-0007 point 5's worked example (a City's 6 direct neighbors, plus depth-2 additions reached only through something directly connected, never laterally); "Curated by me" executes an already-curated list of Relationship ids against the queried Node (the pruning UI itself is a separate, not-yet-scoped View-layer ticket). Both dangling-reference cases (a Relationship's other endpoint gone, or the queried root Node itself gone) resolve to an empty/partial result rather than throwing, per ADR-0007 point 8's no-cascade philosophy. 19 unit tests (`traversal.test.ts`), exported from a new `src/core/query/index.ts` barrel. Picked up a prior attempt left mid-task by a rate limit; found and fixed one real bug while verifying it against the ADR — a cycle's or a direct neighbor's back-edge to the root Node was incorrectly re-inserted into the `nodes` result at depth 2 (the root must stay excluded from `nodes`, though the back-edge itself correctly stays in `relationships`), caught by the suite's own `A -> B -> C -> A` cycle-safety test.
- ADAPT-007 (issue #45): implements the Relationship-authoring UI ADR-0010 decided. `src/adapters/foundry/relationship-authoring-window.ts` adds a dedicated `ApplicationV2` window (not a `DialogV2.prompt`) with two native `<document-tags single>` drop zones (`type` omitted, since a Node is backed by either an Actor or a JournalEntryPage), reached via a "New Relationship…" `getHeaderControls*` button on both Actor sheets (`getHeaderControlsActorSheetV2`) and JournalEntryPage sheets (`getHeaderControlsJournalEntryPageSheet`, researched against Foundry v14's documented class hierarchy — not live-verified, flagged for Alberto). Five new pure, fully unit-tested support modules: `relationship-node-resolution.ts` (reuses `mapActorToNode`/`mapJournalEntryPageToNode` for Node-type resolution, no new classification logic), `relationship-definition-options.ts` (Definition-`<select>` filtering — ineligible options shown disabled-with-reason, not hidden), `relationship-cardinality.ts` (the live, warn-never-block cardinality check via `StorageProvider.getRelationshipsForNode`), `relationship-symmetry.ts` (symmetric-vs-asymmetric endpoint labels and confirmation text), and `relationship-definitions-seed.ts` — a clearly-flagged **temporary placeholder** `RelationshipDefinition[]` (`resides-in`/`resident-of`, `ally-of`, `member-of`, drawn from already-documented vocabulary) standing in until `RelationshipDefinition` persistence exists (CORE-004's deferred fast-follow). Save constructs and persists a real `Relationship` via `StorageProvider.saveRelationship`. Wired into `module-entry.ts` at `init`; exported from `src/adapters/foundry/index.ts`. 52 new unit tests (251 total); `tsc --noEmit`/`eslint`/`build:foundry-module` all clean. The interactive drop/select/save DOM flow itself needs live Foundry verification before Alberto trusts it — see `docs/SESSION_LOG.md`'s 2026-09-06 ADAPT-007 entry and this file's own top-level doc comment for the specific unverified points.

### Fixed

- STORE-003 reviewer pass, product-owner decision (2026-09-06): `to-portable-snapshot.ts`'s `PortableNode`/`PortableRelationship` were silently dropping `history`/`blocks`/`references` — three of `KnowledgeElement`'s nine fields — contradicting ADR-0008 point 6's "full, unredacted snapshot." Alberto's call: keep them, since the export exists to let an external AI or human agent reconstruct the whole world from it, not just titles/tags. Both types now carry all nine fields (`HistoryEntry.timestamp`'s `Date` reshaped to an ISO string via a new `PortableHistoryEntry` type, the only change made purely for JSON-serializability); the test that had asserted the truncated shape as complete (plus one more instance found by grepping the rest of the tree, `export-snapshot.test.ts`) now asserts the real one. New Decision entry in `03_DOMAIN_MODEL.md`'s Knowledge Element Decisions. Also: `WorkerStorageProvider.close()` now actually calls `Worker.terminate()` after the `close` RPC acks, instead of only sending the message and never releasing the Worker; a new `worker.onerror` handler rejects `init()` and any other in-flight RPC calls instead of hanging forever if the Worker script fails to load; `vite.foundry.config.ts`'s `worker: { format: 'es' }` comment no longer references the already-deleted STORE-002 spike file. 152 unit tests (149 + 3 new).

### Added

- STORE-003 (issue #38): implements the real Storage Provider ADR-0008 decided. `src/core/storage/storage-provider.ts` defines `StorageProvider`, the engine-agnostic port Core/Adapters program against (Node/Relationship CRUD plus `getRelationshipsForNode`, ADR-0007's 1-hop primitive). `src/storage/sqlite/` implements it with the official `@sqlite.org/sqlite-wasm` package and the `opfs-sahpool` VFS: `migration.ts` (ordered, `PRAGMA user_version`-tracked SQL migrations creating `nodes`/`relationships`, real indexes on `origin`/`target`/`type`, deliberately **no** foreign key from `relationships.origin`/`target` to `nodes.id` — ADR-0007's "no cascade, no delete-blocking" enforced by never declaring the FK, plus a `CHECK (origin <> target)`), `row-mapping.ts` (Node/Relationship ⟷ row conversion, JSON-encoding metadata/tags/history/blocks/references — fields nothing queries yet, deliberately not normalized), `sqlite-executor.ts` (a minimal structural interface over the real OO1 `Database` API), `sqlite-storage-provider.ts` (the `StorageProvider` implementation itself, synchronous internally, runs inside the dedicated Worker `opfs-sahpool` requires), and a Worker RPC bridging the async `StorageProvider` contract across that boundary (`worker/protocol.ts`, `worker/rpc-dispatcher.ts`, `worker-storage-provider.ts`, `worker/sqlite.worker.ts`, `create-sqlite-storage-provider.ts` as the real browser entry point). `src/core/storage/to-portable-snapshot.ts` implements ADR-0008's `toPortableSnapshot` — full, unredacted, denormalizing Relationships with inlined origin/target Node titles (`null` for a dangling endpoint per ADR-0007 point 8), stamped with `schemaVersion`/`exportedAt`. `src/adapters/foundry/storage-sync.ts` wires the existing `mapActorToNode`/`mapJournalEntryPageToNode` mappings into real persistence; `export-snapshot.ts` gathers Nodes/Relationships from storage and triggers the export via Foundry's own `saveDataToFile` helper. `module-entry.ts` now opens storage on Foundry's `ready` hook, backfills existing Actors/Journal pages, wires `createActor`/`updateActor`/`createJournalEntryPage`/`updateJournalEntryPage` Hooks going forward, and exposes `game.modules.get('archivexus').api.exportSnapshot()` for a GM to trigger from the console (no dedicated export/View UI yet — out of scope). Deliberately does not wire any delete Hook: Node deletion policy is still an open question (ADR-0007). 149 unit tests total, including `sqlite-storage-provider.test.ts`/`migration.test.ts` run against a real (in-memory, no OPFS) `@sqlite.org/sqlite-wasm` engine via its Node build, not a hand-rolled SQL fake. `npm run build:foundry-module` verified in-sandbox: the Worker still code-splits correctly and resolves its own chunk URL relatively (confirming STORE-002's `base: './'` fix still holds for real Worker code, not just the spike), and the wasm binary ships inlined as a base64 data URI by the package's own browser build (no separate asset path to resolve). `docs/PROJECT.md` item 11 added; `CONTRIBUTING`-style live Foundry/OPFS re-verification and `npm install && npm test`/`npx eslint .`/`npm run build*` on Alberto's own machine are still needed — see `docs/SESSION_LOG.md`.
- CORE-004: `src/core/domain/relationship-definition.ts` implements `RelationshipDefinition` as real Core state (`inverse`, `cardinality`, `symmetry`, `traversalCategory`, an optional `validation` origin/target Node-type allow-list) — `createRelationshipDefinition`/`isRelationshipDefinition` follow the same validating-factory code style as `createNode`/`createRelationship`, but deliberately does **not** compose `KnowledgeElement` (Definitions are not Knowledge Elements, `03_DOMAIN_MODEL.md`). `traversalCategory` is the closed 8-value taxonomy ADR-0007 settled on (`RELATIONSHIP_TRAVERSAL_CATEGORIES`); `cardinality` (`RELATIONSHIP_CARDINALITIES`, one-to-one/one-to-many/many-to-one/many-to-many) and `validation`'s shape are judgment calls, not sourced from any ADR/domain-model text — flagged in the PR/session log. Two cross-field invariants enforced, following directly from what "symmetry"/"inverse" mean (same spirit as CORE-003's self-relationship catch, not mandated by any doc): a symmetric Definition's `inverse` must equal its `name`, and a symmetric Definition can't use an asymmetric cardinality. `resolveRelationshipDefinition` is the minimal "resolve a Relationship's definition by id" query surface this ticket needs — a pure lookup over an in-memory collection, not a registry/storage-backed repository and not the View Query API (both out of scope). 28 unit tests (`relationship-definition.test.ts`), exported from `src/core/domain/index.ts`. `Relationship.definitionId` is unchanged (still a plain string reference, CORE-003).
- `decisions/ADR-0009-foundry-ui-extension-hooks.md`: decides how Archivexus extends Foundry's default UI (ADAPT-003). Native `render*`/`getHeaderControls*` Hooks only — no `libWrapper`, no sheet subclassing/replacement, since both documented hook families already cover every identified need. First concrete surface: a `flags.archivexus.nodeType` tagging control on the Actor sheet via `renderActorSheetV2`, replacing the current console-only workflow (`actor-to-node.ts`'s flag reader is unchanged). `docs/PROJECT.md` item 8 updated. Research-only, per issue #20's own scoping — no UI code yet.
- ADAPT-003 implemented: `src/adapters/foundry/actor-node-type-tag.ts` (`buildNodeTypeTagHTML`, `registerActorNodeTypeTag`) injects the `flags.archivexus.nodeType` tagging control decided in ADR-0009 into the Actor sheet via `renderActorSheetV2`. `KNOWN_NODE_TYPES` offered as `<datalist>` suggestions only (NodeType stays an open string, ADAPT-005). Live-tested after build+reload found a real bug the static pass missed: injecting as `.window-content`'s first child overlapped Foundry's own floating title bar (`.window-header` renders over the top of `.window-content`) — fixed by inserting `afterend` of `.window-header` instead. Confirmed live end-to-end afterward: a typed value round-trips through Foundry's native `submitOnChange` to `actor.flags.archivexus.nodeType`. `ADR-0009` gained a Correction recording the bug and fix. 12 unit tests, exported from `src/adapters/foundry/index.ts`, wired in `module-entry.ts`. `docs/PROJECT.md` item 8 updated.
- ADAPT-003 redesigned: after the permanent-bar implementation above was live-verified working, Alberto challenged its position and its necessity — a grep confirmed nothing in Core reads `Node.type` yet, so the always-visible bar had zero current payoff for the common case (a Player Character already gets the right type for free via `ACTOR_FALLBACK_NODE_TYPE`). Replaced with an opt-in header-control button per Alberto's choice: `src/adapters/foundry/actor-node-type-tag.ts` now registers a `getHeaderControlsActorSheetV2` listener instead of `renderActorSheetV2`, pushing a button that opens a `foundry.applications.api.DialogV2.prompt` dialog (`buildNodeTypeDialogContent`, replacing `buildNodeTypeTagHTML`) only when a GM actively wants to reclassify an Actor; no more persistent DOM injection. Live-verified against Foundry v14.367 + dnd5e 5.3.3 before writing final source, including a load-bearing gotcha: header controls are only recomputed on a fresh render (close+reopen), not a plain re-render. `ADR-0009` gained an Amendment recording the challenge, the finding, and the verification. `docs/PROJECT.md` item 8 updated.
- CORE-003: `src/core/domain/relationship.ts` implements the Relationship instance shape (`origin`/`target` Node ids, `definitionId`, composed from KnowledgeElement exactly like Node) with unit tests, exported from `src/core/domain/index.ts`. Deliberately scoped to the instance only — Relationship Definition (inverse/cardinality/symmetry/validation/traversalCategory as real Core state) stays separate, not-yet-scoped follow-up work. Reviewer pass surfaced an undocumented gap (self-relationships weren't addressed anywhere); resolved as a new `03_DOMAIN_MODEL.md` Decision — a Relationship's origin and target must be distinct Nodes — and enforced in `createRelationship`.
- ADAPT-005 decided: a Foundry `Scene` is never its own Node — it's a `scene`-type Block on whatever Node the GM explicitly links it to (never inferred, never force-created if unlinked). Also settles Block's first real shape: `{ type, uuid, title? }`, always a reference to a real Foundry element (replacing `Block.data`'s `unknown` placeholder) — `03_DOMAIN_MODEL.md`'s Knowledge Element and Node Decisions sections updated, `02_LANGUAGE.md`'s Block entry updated to match. Decide-only; implementing the mapping and Block's real TS type is separate follow-up work.
- STORE-002 spike: verified `opfs-sahpool` + a dedicated Worker work inside a real Foundry client session (`@sqlite.org/sqlite-wasm`), closing ADR-0008 point 8's previously-open feasibility risk (see its Addendum). Found and fixed a real bug along the way: Vite's default `base: '/'` baked absolute root-relative URLs into Worker/asset chunks, which 404 against Foundry's actual nested serving path — `vite.foundry.config.ts` now uses `base: './'`. Also adds `scripts/release-foundry-module.sh` to automate the manual parts of ADR-0006's release step (build, zip, tag, `gh release create`) without changing that it stays human-run.
- `decisions/ADR-0008-storage-provider.md`: decides Archivexus's Storage Provider (STORE-001). Primary store is embedded SQLite (WASM, `@sqlite.org/sqlite-wasm`, `opfs-sahpool` VFS, with an IndexedDB-backed or `sql.js` fallback), satisfying ADR-0007's 1-hop-lookup and no-cascading-FK constraints via real indexes. Standalone/external-consumer reachability (e.g. a future Web UI, or handing the campaign's knowledge graph to an external AI agent for planning) is solved by a deliberate, user-triggered export producing a generated JSON snapshot — never a live or continuous sync — denormalized (Relationships carry inlined origin/target titles) and stamped with `schemaVersion`/`exportedAt`. The snapshot-shaping logic lives in Core (`toPortableSnapshot`), not the Adapter. Export is a full, unredacted snapshot by product-owner decision (Visibility gates what a player sees, not the GM's own access); a visibility-filtered export is named as a separate, not-yet-built future feature. One implementation-risk item is named but not resolved: whether dedicated-Worker + `opfs-sahpool` mechanics work cleanly inside a real Foundry client sandbox, flagged as a required spike before implementation. Reached via the same architect/dba (plus a product-owner scope call) independent-convergence-then-discuss-with-each-other process as ADR-0007. `01_ARCHITECTURE.md`'s Storage section and `docs/PROJECT.md`'s Current priorities updated to match.
- `decisions/ADR-0007-relationship-view-traversal.md`: how a View selects which connected Relationships/Nodes to include when generated or expanded (e.g. clicking a City in a Graph View). Relationship Definition gains a `traversalCategory` (small closed taxonomy: location, affiliation, kinship, conflict, governance, participation, ownership, narrative); a View stores a declarative spec (categories + depth) resolved by a new Core Query API, engine-agnostic and shared by every View format. MVP scope is 3 fixed presets (Direct only / Everything connected, depth 2 / Curated by me), not open per-category config. Also resolves that a Relationship may survive its origin/target Node being deleted (no cascade, no delete-blocking — traversal excludes it naturally). Reached by consulting architect, product-owner, dba and ux-ui-designer independently, who converged on the same shape unprompted; `03_DOMAIN_MODEL.md`'s Relationship and View Decisions sections updated to match, plus `02_LANGUAGE.md`'s Relationship Definition examples.
- Initial project vision.
- Initial architecture documentation.
- Language specification.
- Architecture Decision Records (ADR-0001, ADR-0002, ADR-0003, ADR-0004, ADR-0005).
- Operational project snapshot (`docs/PROJECT.md`).
- ADR workflow documentation (`docs/decisions/README.md`, `docs/decisions/ADR-0000-template.md`).
- Claude Code subagents for product ownership, UX/UI design, development, QA, and documentation review (`.claude/agents/`).
- Claude Code subagents for cross-cutting system architecture (`architect.md`) and database administration (`dba.md`), and `reviewer.md` updated to route findings to them.
- Project scaffolding: `package.json`, `tsconfig.json`, `vitest.config.ts`, `eslint.config.js`, `.prettierrc.json` (TypeScript, Vite, Vitest, ESLint, Prettier per `docs/PROJECT.md`'s planned stack).
- Core domain: `KnowledgeElement` base abstraction (`src/core/domain/`) — the shared shape every Node, Relationship and View will compose with, plus its supporting value types (`Visibility`, `Tag`, `HistoryEntry`, `Block`, `KnowledgeElementReference`), a validating `createKnowledgeElement` factory enforcing the Domain Invariants from `03_DOMAIN_MODEL.md`, and unit tests (CORE-001).
- `docs/CONTRIBUTING_GUIDE.md` Rule 11: the feature-branch + rebase + squash git workflow is now a binding project rule, not just something one agent happens to mention. Updated to also list `qa-tester` once it got the same section, and to state the actual criterion (any role with `Bash`) instead of a hardcoded role list that needs manual upkeep each time.
- `.claude/agents/dba.md` (and its `agents-core` canonical copy): added the same `## Git workflow` section as `software-developer.md`, plus a DBA-specific note on not squashing across already-applied migrations — product-owner call, since DBA has the same Bash/Write/Edit footprint as software-developer and was missing it.
- `.claude/agents/qa-tester.md` (and its `agents-core` canonical copy): added the same `## Git workflow` section — qa-tester has `Bash` + `Write` (writes automated tests per its own Deliverable mode) and was missing it too. `architect.md` was considered and correctly excluded: it has `Write` but no `Bash`, so it has no way to execute the workflow itself.
- Core domain: `Node` (`src/core/domain/node.ts`) — the first concrete Knowledge Element type, composed on top of `createKnowledgeElement` with one added field (`type`). `NodeType` is a plain string, not a closed enum, per `01_ARCHITECTURE.md`'s Extensible principle; `KNOWN_NODE_TYPES` lists `03_DOMAIN_MODEL.md`'s examples as a reference only. Enforces Node's Domain Invariants (no nesting, no type change, id independent of type) and unit tests covering them (CORE-002).
- Foundry Adapter: `mapJournalEntryPageToNode` (`src/adapters/foundry/journal-entry-page-to-node.ts`) — maps a Foundry `JournalEntryPage` to a Node (ADAPT-001). Uses the page's `uuid` as the Node id (ADR-0001) and `name` as its title. Node type comes from an explicit `flags.archivexus.nodeType` the GM sets, falling back to the generic `Lore` type when absent — deliberately no content/title-based inference, since guessing a semantic type from free text would be business logic an Adapter must not contain (`01_ARCHITECTURE.md`'s Adapters section). Visibility comes from the page's `ownership.default` per 02_LANGUAGE.md/ADR-0003's mapping (NONE/LIMITED → hidden, OBSERVER → visible, OWNER → owned); an unset or unrecognized value (including Foundry's `-1` "inherit from parent" sentinel) falls through to Node's own default (hidden) rather than being resolved here — resolving inheritance against the parent `JournalEntry` needs a live Foundry instance and is out of scope for this pure mapping function. Page content/text is deliberately not mapped to Blocks yet — `Block` is still an explicitly provisional placeholder with no settled shape. Pure and Foundry-runtime-free (a minimal structural `FoundryJournalEntryPageLike` type stands in for the real Foundry type, so this is unit-testable without a Foundry install or a types dependency), with unit tests covering id/title/type/visibility mapping and the fallback cases.
- `docs/decisions/ADR-0006-foundry-module-distribution.md`: the Foundry Adapter's build/bundle is split from the rest of the codebase (a dedicated `vite.foundry.config.ts` target), distribution is manifest-based (Foundry fetches `module.json` and a release archive itself — the real install flow, not a local folder copy), and installing a module into a live Foundry world is always a manual, human (GM) action — never performed by an agent.
- Foundry module scaffold (ADAPT-002): `module.json` (manifest pointing at `dist/foundry/archivexus.js`, `manifest`/`download` URLs per ADR-0006), `src/adapters/foundry/module-entry.ts` (the Foundry-loadable entry point, registers on Foundry's `init` hook — doesn't read/write any Foundry documents yet), `src/adapters/foundry/foundry-globals.d.ts` (minimal ambient `Hooks`/`console` declarations, same no-real-Foundry-types-dependency tradeoff as `journal-entry-page-to-node.ts`), and a `build:foundry-module` npm script. Building the actual bundle still needs to be run locally (this session's environments can't run `vite build` — see ADR-0006's Context).
- `src/adapters/foundry/logger.ts`: a tiny structured logger (`createLogger(moduleId)` → `info`/`warn`/`error`, each prefixed with the module id) replacing `module-entry.ts`'s raw `console.log` — centralizes the package's only `console` usage to one file and makes it mockable, with `logger.test.ts` covering the prefixing behavior.
- Foundry Adapter: `mapActorToNode` (`src/adapters/foundry/actor-to-node.ts`) — maps a Foundry `Actor` to a Node (ADAPT-004), same pattern as ADAPT-001: `uuid` as id, `name` as title, an explicit `flags.archivexus.nodeType` GM flag as type (falling back to `Character`, `ACTOR_FALLBACK_NODE_TYPE`), `ownership.default` mapped to Visibility per 02_LANGUAGE.md/ADR-0003. Deliberately does not branch on Foundry's own `actor.type` (confirmed from the real "Academia El Último Norte" campaign to be character/npc/encounter/group) — a product-owner call on issue #22, applying ADAPT-001's no-content-inference rule consistently. Validated against 4 real Actors, one per real `actor.type`, pulled live via Claude in Chrome. Re-exported from `src/adapters/foundry/index.ts` (page mapping's `FALLBACK_NODE_TYPE` left as-is; Actor's is `ACTOR_FALLBACK_NODE_TYPE` to avoid a barrel-export name collision).
- `KNOWN_NODE_TYPES` (`src/core/domain/node.ts`) gained `Vehicle`, added in the same three places that must stay in sync (`node.ts`, `docs/03_DOMAIN_MODEL.md`, `docs/02_LANGUAGE.md`'s Node examples) — prompted by Foundry's own Actor types including `vehicle` (dnd5e), which doesn't fit any of the 10 existing Node types well. Documentation-only: `NodeType` stays a plain string, not a closed enum, per `01_ARCHITECTURE.md`'s Extensible principle.

### Changed

- Synthesized inline documentation across `src/core/domain/` and `src/adapters/foundry/` — several files were up to 85% comment lines, restating rationale already captured in `03_DOMAIN_MODEL.md`, the ADRs, and this file. Condensed to the non-obvious 'why' plus a pointer to where the full rationale lives; no logic changes (verified by diffing comment-stripped output against `HEAD`, plus a clean `tsc --noEmit`).

- `.claude/agents/software-developer.md` (and its canonical copy in `agents-core`): added a `## Git workflow` section — feature branches off trunk, rebase (not merge) to stay current, squash before merging back — per user feedback after CORE-001.
- Reconciled `01_ARCHITECTURE.md`, `02_LANGUAGE.md` and `03_DOMAIN_MODEL.md` for consistency (single source of truth for Storage, Visibility vocabulary, Relationship-endpoint rules).
- Clarified when a decision needs a full ADR vs. a lightweight entry in `03_DOMAIN_MODEL.md` (`docs/CONTRIBUTING_GUIDE.md`, `CONTRIBUTING.md`).
- Updated `README.md`'s documentation table and Planned Architecture diagram to match the actual `docs/` contents.
- Resolved all of `03_DOMAIN_MODEL.md`'s Outstanding Questions: Foundry Journal → Node mapping (with a generic `Lore` type), Blocks made explicitly optional, Knowledge Element capabilities exposed directly rather than through composable behaviors, and View promoted to a first-class Knowledge Element (see ADR-0005).
- Reconciled View's format/audience vocabulary between `02_LANGUAGE.md` and `03_DOMAIN_MODEL.md` — audience is now expressed via Visibility instead of a separate "GM/Player/Public View" vocabulary.
- `README.md`'s Current Status now reflects that implementation has started, instead of still saying pre-implementation. Its Repository Structure no longer shows a separate top-level `tests/` folder — tests are colocated as `*.test.ts` next to what they test, now also stated in `CONTRIBUTING.md`'s Coding Standards.
- `docs/PROJECT.md`: Stage and Current priorities updated now that CORE-002 (`Node`) is merged to `dev` (board card moved to Done, issue #9 auto-closed); ADAPT-001 (Foundry Adapter) moved to "Ready for Implementation" on the board as the next task.
- `docs/PROJECT.md`: Current priorities updated to reflect ADAPT-002 merged and verified live, and two new backlog items scoped: ADAPT-004 (map Foundry `Actor` to Node, issue #22) and ADAPT-005 (decide how Foundry `Scene`s map into Archivexus — an open domain question, not yet an implementation ticket, issue #23), both ahead of ADAPT-003 in sequence per the user's priority to import their real campaign next.
- `docs/PROJECT.md`: Current priorities updated now that ADAPT-004 is implemented and #25 is fixed (both struck through as done); ADAPT-005 now notes 22 real Scenes are available to look at, and ADAPT-003 notes ux-ui-designer's GM-tagging-flow proposal is waiting as design input.
- `docs/PROJECT.md`: Stage and Current priorities updated after verifying the full real dataset (all 41 real Actors + all 61 real Journal Pages) through the actual `mapActorToNode`/`mapJournalEntryPageToNode` functions — 102 Nodes, 0 title collisions today, versus 21 records across 6 real collision groups that #25 fixed. New backlog item added: STORE-001 (issue #32), deciding Archivexus's Storage Provider — scoped and ready to start next session, deliberately excluding implementation.

### Fixed

- `docs/CONTRIBUTING_GUIDE.md` Rule 11 pointed contributors at `.claude/agents/`'s `## Git workflow` sections for branch-naming and force-push detail — stale as soon as `.claude/agents/` stopped being tracked (reviewer catch). Inlined that detail into Rule 11 itself, which is now self-contained; the agent files mirror it for Claude Code sessions instead of being the source of it.

- `docs/PROJECT.md`'s Stage section still said ADAPT-002 was "not yet built or released," contradicting Current priorities item 4 a few lines below, which correctly states it's built, released and confirmed live at `localhost:30000` — caught by reviewer, Stage reworded to match.
- `mapJournalEntryPageToNode` used only `page.name` for a Node's title; real campaign data showed 5 distinct NPCs' generically-named "Retrato" (Portrait) pages collapsing to the exact same Node title (#25). `FoundryJournalEntryPageLike` gained an optional `parent.name`; the title is now qualified as `"{parent.name} — {name}"` when that disambiguates anything, falling back to the bare page name otherwise (backward compatible). Re-verified against the real 5-record case: 5 distinct titles now, confirmed.
- `package.json` declared `"license": "MIT"`; the repo's actual `LICENSE` is Mozilla Public License 2.0 — corrected to `"MPL-2.0"`.
- Stale `0000-template.md` references corrected to `ADR-0000-template.md` (the file's actual name) in `architect.md`, `software-developer.md` and `SESSION_LOG.md` — same in `agents-core`'s canonical `architect.md`/`software-developer.md`. Same recurring wrong-citation pattern flagged in the 2026-08-27 session; reviewer caught these via a repo-wide grep.
- `docs/02_LANGUAGE.md`'s Node examples list was missing `Item` (present in `03_DOMAIN_MODEL.md`'s Node section and `KNOWN_NODE_TYPES` in `src/core/domain/node.ts`, both of which cite `03_DOMAIN_MODEL.md`) — added, and reordered to match the other two lists exactly.
- `README.md`'s Contributing section and `CONTRIBUTING.md`'s opening still claimed the project was in an "early architecture phase"/pre-implementation, contradicting `README.md`'s own already-fixed Current Status section and `docs/PROJECT.md`'s Stage — same recurring stale-claim pattern as the `0000-template.md` citations; caught by reviewer, both updated to reflect that implementation is underway.
- `02_LANGUAGE.md`'s and `03_DOMAIN_MODEL.md`'s Node sections described the Foundry Adapter as typing a page "by what it actually describes" — read as automatic content-based inference, contradicting ADAPT-001's actual implementation (an explicit GM-set flag, never inferred, per `01_ARCHITECTURE.md`'s "Adapters should contain no business logic"). Both reworded to describe the real GM-flag mechanism.
- Wrong citation, same recurring pattern: `journal-entry-page-to-node.ts`'s comments, `CHANGELOG.md`, and `docs/SESSION_LOG.md` attributed "Adapters must contain no business logic" to `CONTRIBUTING_GUIDE.md` Rule 3 (which is actually about Core platform-agnosticism, not Adapters). The line is in `01_ARCHITECTURE.md`'s Adapters section — corrected in all three places.
- `docs/PROJECT.md`: Stage and Current priorities updated now that ADAPT-001 is merged to `dev` (board card moved to Done, issue #10 auto-closed).
- `module-entry.ts` failed `npm run lint`: `no-undef` flagged `Hooks` and `console` because ESLint's `no-undef` rule doesn't read the ambient declarations in `foundry-globals.d.ts`, only its own `globals` config (reviewer caught this by actually running `eslint`, not just `tsc`). Fixed by adding `eslint.config.js`'s `FOUNDRY_ADAPTER_GLOBALS` list (currently just `Hooks`) plus `console`, scoped to `src/adapters/foundry/**/*.ts` and explicitly documented to be kept in sync with `foundry-globals.d.ts`.

### Removed

- `.claude/agents/*.md` (7 subagent personas) untracked from git — kept on disk for local use, but a repo shouldn't require an external contributor to adopt this project's specific agent tooling. `.claude/agents/` added to `.gitignore`; past commits still contain them (no history rewrite on a shared trunk branch).

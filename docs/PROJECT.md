# Project

> The operational snapshot of this project. This is the file agents read first, and the one to update most often — whenever the stage, priorities, or constraints actually change.

## Name

Archivexus

## Type

- [x] Personal / fun project

Confirmed 2026-08-26: built for personal use in tabletop campaigns, no commercial goal for now. Agents should favor speed and simplicity over process here.

## Stage

Implementation started (2026-08-29). Domain model and architecture are settled (see `03_DOMAIN_MODEL.md`'s Decisions and ADRs); the Core is now being built out incrementally. `src/core/domain/` has the `KnowledgeElement` base abstraction (CORE-001) and `Node` (CORE-002), both merged to `dev` with unit tests. `src/adapters/foundry/` has the first Foundry Adapter slice (ADAPT-001, merged to `dev`): `mapJournalEntryPageToNode`, a pure JournalEntryPage-to-Node mapping. A real, installable Foundry module scaffold now exists too (ADAPT-002, per ADR-0006): `module.json`, `src/adapters/foundry/module-entry.ts`, and a dedicated `build:foundry-module` build target — built, released and manually installed by the user; confirmed loading in a live Foundry v14 GM session at `localhost:30000` (building itself is still a manual, locally-run step, since neither of this session's environments can run `vite build` — see ADR-0006's Context). `package.json`/`tsconfig.json`/`vitest.config.ts`/`eslint.config.js` scaffold the TypeScript/Vite/Vitest/ESLint/Prettier stack from `README.md`. Real Foundry data is now flowing through both Adapter slices: all 41 real Actors and all 61 real Journal Pages from the "Academia El Último Norte" campaign map cleanly to Nodes (verified 2026-08-30 — see `SESSION_LOG.md`). What's still missing is a place to persist that output and a View to look at it through. Storage is now decided (STORE-001, issue #32, `decisions/ADR-0008-storage-provider.md`, 2026-08-31): embedded SQLite as the primary store, with a deliberate user-triggered export (a generated JSON snapshot) as the reachability path for a future standalone frontend — read-only for now, motivated by wanting to hand the campaign's knowledge graph to an external AI agent for planning. `01_ARCHITECTURE.md`'s Application layer already has room for that standalone client ("Web UI" alongside Foundry), so it doesn't change the Core. That Worker/OPFS feasibility spike is now resolved (STORE-002, 2026-08-31, see ADR-0008's addendum). `Relationship` (Core domain, CORE-003, 2026-08-31) is also now implemented and merged, instance-only (see `03_DOMAIN_MODEL.md`'s Decisions). ADAPT-003 (Foundry UI extension/customization, issue #20) is now done and merged to `dev`, as an opt-in header-control button rather than the originally-shipped permanent bar — see item 8 below and `decisions/ADR-0009-foundry-ui-extension-hooks.md`'s Amendment. The real Storage Provider is now implemented and live-verified (STORE-003, issue #38, 2026-09-06): `src/core/storage/storage-provider.ts` defines the engine-agnostic `StorageProvider` port Core/Adapters program against; `src/storage/sqlite/` implements it with the official `@sqlite.org/sqlite-wasm` package (`opfs-sahpool` VFS, real SQL schema/migrations via `PRAGMA user_version`, real indexes for ADR-0007's 1-hop lookup, deliberately no FK on `Relationship.origin`/`target` per ADR-0007/ADR-0008); `src/core/storage/to-portable-snapshot.ts` is the Core-owned, full/unredacted JSON export ADR-0008 settled on. The Foundry Adapter's existing mappings (`mapActorToNode`, `mapJournalEntryPageToNode`) now actually persist via `src/adapters/foundry/storage-sync.ts`, wired into real Foundry Hooks (`createActor`/`updateActor`/`createJournalEntryPage`/`updateJournalEntryPage`, plus a `ready`-time backfill) in `module-entry.ts` — confirmed live in a real Foundry reload: a 94KB SQLite file persisting to OPFS with clean console logs. `RelationshipDefinition` is now real Core state too (CORE-004, issue #39, 2026-09-06): `src/core/domain/relationship-definition.ts` implements `inverse`/`cardinality`/`symmetry`/`traversalCategory`/an optional `validation` Node-type allow-list, per ADR-0007 — deliberately Core-domain-only (no storage persistence yet); `Relationship.definitionId` stays an unchanged plain string reference. Current focus: with STORE-003 and CORE-004 both merged, product-owner scoped the path to Alberto's stated goal ("a visual interface for me to start defining relations in Foundry so when we select something — city, actor, scene — we can bring the graph to life") into three new tickets: **CORE-005** (issue #42, implement-only — the Core Query API executing ADR-0007's 3 traversal presets, fully unblocked now that STORE-003/CORE-004 exist), **ADAPT-006** (issue #43, decide-only — how a GM actually authors a Relationship instance between two Nodes in Foundry; explicitly not assumed to reuse ADAPT-003's single-document tagging pattern, since picking two arbitrary entities is a materially different shape), and **VIEW-001** (issue #44, decide-only — the graph View's rendering/interaction design, including ADR-0007's own named open item on content-prominence ordering). Sequencing: ADAPT-006 is the very next action (ux-ui-designer) since it directly blocks Alberto's literal ask and has zero design decided yet; CORE-005 (software-developer) has no dependency on ADAPT-006's outcome and can slot in alongside it; VIEW-001's design pass can start in parallel but its implementation is last, needing both CORE-005 and real authored Relationship data to be meaningful. **ADAPT-006 (issue #43) is now decided (2026-09-06, `decisions/ADR-0010-relationship-authoring-ui.md`):** a dedicated `ApplicationV2` window, entered via a `getHeaderControls*` button (ADR-0009's proven entry-point mechanism, reused only for the single-document click) on an Actor's or JournalEntryPage's own sheet, with two native `<document-tags single>` drop zones (no `type` restriction, since a Node is backed by either document type) accepting Foundry's native drag-and-drop (sidebar directory, or dragging an already-open sheet's title bar) or a pasted UUID — confirmed via fresh v14 API research that no generic cross-type document-picker exists to reuse instead. Node-type resolution reuses the existing pure `mapActorToNode`/`mapJournalEntryPageToNode` mappings (no storage round-trip). `RelationshipDefinition` selection filters (shows disabled with a reason, doesn't hide) by `validation.allowedOriginTypes`/`allowedTargetTypes`; a cardinality conflict (checked via STORE-003's existing `getRelationshipsForNode` 1-hop lookup, no CORE-005 dependency) warns but never blocks, a deliberate authoring-time-vs-storage-time distinction from ADR-0007 point 8's delete-time no-blocking rule. Symmetric Definitions (`symmetry: true`) drop the Origin/Target labeling for neutral, order-independent ones. One real dependency this ADR surfaces but doesn't resolve: `RelationshipDefinition` still has no storage persistence (CORE-004's deferred fast-follow), so the implement ticket needs a temporary seeded/hardcoded Definition list to have anything to populate the picker from. Implementation itself is separate, not-yet-scoped follow-up work. **CORE-005 (issue #42) is now implemented (2026-09-06):** `src/core/query/traversal.ts` is the engine-agnostic Core Query API resolving ADR-0007's 3 fixed MVP presets (`resolveTraversal`, `TRAVERSAL_PRESETS`) against `StorageProvider`'s `getRelationshipsForNode` 1-hop primitive — "Direct only" (depth 1), "Everything connected" (Core-composed BFS to `EVERYTHING_CONNECTED_DEPTH = 2`, per the ADR's worked example), and "Curated by me" (executes an already-curated list of Relationship ids against the queried Node, the pruning UI itself being out of scope for this ticket). This picked up a prior attempt left mid-task by a rate limit (untracked `traversal.ts`/`traversal.test.ts`/`index.ts`, otherwise substantially complete); cross-checking it against ADR-0007 point by point found one real bug in the depth-2 composition: a cycle's or a direct neighbor's back-edge to the root Node (e.g. C→A in an A→B→C→A cycle) was re-inserted into the `nodes` result once expanded at depth 2, even though the root must be excluded from `nodes` (the back-edge itself correctly stays in `relationships`, since it's still a real fact) — caught by the suite's own explicit `A -> B -> C -> A` cycle-safety test, fixed with a one-line guard. Both dangling-reference cases (a Relationship's other endpoint gone, and the queried root itself gone) return an empty/partial result rather than throwing, per ADR-0007 point 8. 19 unit tests, exported from a new `src/core/query/index.ts` barrel (matching `src/core/domain/index.ts`/`src/core/storage/index.ts`'s per-folder pattern). Deliberately does not filter/group by `traversalCategory` — no MVP preset restricts by category — and never touches `src/adapters/foundry/` (out of scope, ADAPT-007 runs concurrently there). `npm install`, `tsc --noEmit`, `npx eslint .` clean; `npx vitest run` 199/199 (180 pre-existing + 19 new), no regressions. **ADAPT-007 (issue #45) now implements it (2026-09-06):** `src/adapters/foundry/relationship-authoring-window.ts` (the `ApplicationV2` window plus both `getHeaderControls*` registrations — `getHeaderControlsJournalEntryPageSheet`'s exact name researched against Foundry v14's documented class hierarchy, not yet live-verified) and five new pure, fully unit-tested support modules (Node-type resolution, Definition filtering, the cardinality check, symmetric/asymmetric display text, and a clearly-flagged temporary seeded `RelationshipDefinition[]` standing in until CORE-004's deferred persistence fast-follow lands). Save persists a real `Relationship` via `StorageProvider.saveRelationship`. 52 new unit tests (251 total), `tsc`/`eslint`/`build:foundry-module` all clean. **Live-verified in a real Foundry session (2026-09-06):** both header-control entry points confirmed present; Origin's `value="<uuid>"` pre-fill confirmed working; a real Foundry drag-drop payload dispatched at the Target `<document-tags>` element confirmed setting `.value` and firing the `change` event the code depends on; Definition filtering confirmed showing `resides-in`/`member-of` disabled-with-reason against two Characters while leaving `ally-of` selectable; picking it produced the correct symmetric summary text and enabled Save. Stopped short of clicking Save itself (no delete-Relationship UI exists yet to clean up test data afterward — see ADAPT-008, issue #48, opened as the next ticket). **Merged to `dev`** (fast-forward, no conflicts). **A real, concrete scenario then surfaced a gap in the Node model itself (2026-09-06, architect, `decisions/ADR-0011-journal-entry-page-node-attachment.md`):** Alberto's "Lord Fausto Farcon" is 4 disconnected Foundry documents today (3 JournalEntryPages plus an Actor) with no way to unify them into one clickable thing, since the existing Node Decision maps every page to its own standalone Node unconditionally. Two parallel consults (product-owner: extend ADAPT-005's Scene→Block precedent to JournalEntryPage; ux-ui-designer: a new symmetric `same-entity-as` RelationshipDefinition authored via ADAPT-007) gave conflicting answers; Alberto's own steer (a single per-page authoring-time choice, not a Relationship authored per fragment) settled the direction, formalized as ADR-0011: a GM-set `flags.archivexus.attachedToNodeId` flag (extending ADR-0009's existing `DialogV2` tagging control to JournalEntryPage sheets, adding a `<document-tags single>` field alongside the existing type input) makes a page become a `{type: 'JournalEntryPage', uuid, title}` Block on the target Node (Actor- or JournalEntryPage-backed, no restriction) instead of its own Node. Retagging an already-synced standalone page deletes its Node; any Relationships already authored against it survive as dangling references per ADR-0007 point 8's existing answer (reused, not reinvented), surfaced via a non-blocking warning. No forced migration of the 61 already-backfilled pages — opt-in and opportunistic only. `same-entity-as` is judged superseded for this problem, not kept as a parallel mechanism — see the ADR's Alternatives Considered. Decide-only pass; implementation (the dialog extension, `storage-sync.ts`'s attach-orchestration logic, and a DBA follow-up on the Block-upsert/detach-cleanup schema mechanics named as open in the ADR's Consequences) is separate, not-yet-scoped work. Other open backlog candidates, lower priority: persisting `RelationshipDefinition` now that the Storage Provider exists (fast-follow, deliberately left out of CORE-004), the Scene→Block mapping (ADAPT-005 decided, not implemented), Node deletion policy (still explicitly open per ADR-0007), a real export-trigger UI (today it's a console-only `game.modules.get('archivexus').api.exportSnapshot()` call), and ADAPT-008 (delete a Relationship instance, issue #48, no design yet). **ADR-0011 is now fully implemented (ADAPT-009, issue #49, 2026-09-06, on `feat/adapt-009-journal-entry-page-attachment`, not yet merged):** `Block`'s real shape (`{ type, uuid, title? }`, `src/core/domain/block.ts`) replaces CORE-001's `{ id, type, data }` placeholder, with a validating `createBlock`/`isBlock` factory matching `createNode`/`createRelationship`'s style — confirmed via grep, and fixed the 3 test fixtures that still used the old shape, that nothing outside `block.ts` read the old fields. Two new pure Core helpers on `src/core/domain/node.ts` (`upsertBlockByUuid`, `removeBlockByUuid`, DBA's Amendment) transform a Node's `blocks` array with zero Foundry dependency. `src/adapters/foundry/journal-entry-page-node-tag.ts` extends ADR-0009's `DialogV2` tagging control to JournalEntryPage sheets via `getHeaderControlsJournalEntryPageSheet` (confirmed against Foundry v14's official API docs — the real class hierarchy `ApplicationV2 → DocumentSheetV2 → JournalEntryPageSheet → JournalEntryPageHandlebarsSheet → JournalEntryPageTextSheet`, plus the hook's own documented "fires up the inheritance chain" behavior — still not live-render-verified; coexists with `relationship-authoring-window.ts`'s own listener on the same hook name, both push into the same `controls` array). The dialog gains a second `<document-tags single>` field for attaching to an existing Node; judgment call made (flagged, not silent): rather than an interactive radio toggle, both fields stay visible/enabled and precedence is resolved once at Save time (attach wins if its field resolves to a uuid), exactly matching ADR-0011 point 1's own "mutually exclusive at read time" text, avoiding extra untested live-DOM/event-wiring surface. `src/adapters/foundry/storage-sync.ts`'s `syncJournalEntryPage` now resolves `flags.archivexus.attachedToNodeId` (via a new pure `resolvePageAttachment` in `journal-entry-page-to-node.ts`): attach-and-resolves deletes the page's standalone Node and upserts a Block onto the target; attach-but-dangling falls back to standalone, never to nothing; unset stays standalone, unchanged. An unconditional, ungated `listNodes()` cleanup scan runs on every call, removing any stale Block referencing this page from a Node that isn't the current legitimate target (DBA's Amendment, mechanic 2) — this also correctly handles detach-to-standalone. 47 new unit tests (298 total); `npm install`, `tsc --noEmit`, `npx eslint .`, `npx vitest run`, `npm run build:foundry-module` all clean in-sandbox. Committed on its feature branch, not merged to `dev`, not pushed — needs live Foundry verification (the same discipline every prior `getHeaderControls*` addition in this lineage has needed) before Alberto trusts it. **Two follow-up commits on the same branch (ADAPT-010, issue #50, 2026-09-06, item 20 below) closed out the one named gap and a real test-coverage gap a reviewer pass caught:** `storage-sync.ts` now implements ADR-0011 point 5's non-blocking `ui.notifications.warn`, checking `getRelationshipsForNode` before deleting a page's standalone Node and warning the GM by name/count if any exist, without blocking the delete/attach; and a new `header-controls-coexistence.test.ts` actually exercises `journal-entry-page-node-tag.ts`'s and `relationship-authoring-window.ts`'s `getHeaderControlsJournalEntryPageSheet` registrations together against one shared mock `Hooks`, confirming both really do coexist (2 independent listeners, both pushing into the same `controls` array) rather than resting on each module's own isolated test. 300 unit tests total; `npm install`, `tsc --noEmit`, `npx eslint .`, `npx vitest run` all clean, no regressions. **CORE-005 still has zero UI consumer — formalized as its own decided interim ticket (ux-ui-designer, 2026-09-06, `decisions/ADR-0012-node-connections-panel.md`):** a narrow "Node Connections panel" (proposed issue: `ADAPT-011`), entered via the same `getHeaderControls*` pattern as ADAPT-003/006/009, showing only "Direct only" traversal results grouped by `traversalCategory`, sorted degree-descending/alpha-tiebreak (a real, scoped answer to ADR-0007 point 6's open content-prominence question), with a no-Actor-yet row badge and inline clickable Blocks per row. This formalizes an earlier, never-written-up brainstorm sketch and updates it for ADR-0011: that sketch's plan to collapse `same-entity-as` clusters into one row is retracted entirely, since ADR-0011's page-attachment mechanism already deduplicates any explicitly-attached page as a Block on its target Node, needing no clustering logic at all. Deliberately excludes "Everything connected," "Curated by me," any persisted View arrangement, and the real graph/canvas rendering — all explicitly deferred to VIEW-001 (issue #44), which this ADR does not touch. Decide-only; needs nothing new from `StorageProvider`/CORE-005. **ADR-0013 (Relationship deletion UI) is now implemented (ADAPT-012, issue #53, 2026-09-06, on `feat/adapt-012-relationship-deletion-ui`, not yet merged):** `src/adapters/foundry/relationship-list-window.ts` (new file) is the `ApplicationV2` list window ADR-0013 decided — a third `getHeaderControls*` button, "Relationships…", pre-scoped to the clicked document's own Node id via `relationship-node-resolution.ts`'s existing `resolveDroppedDocumentNode` (no drop step needed, only one entity involved). `_renderHTML` fetches `storage.getRelationshipsForNode(nodeId)` and renders each row's already-persisted `title` verbatim via a pure `buildRelationshipListContentHTML`/`buildRelationshipListRowsHTML` pair (the empty state, "No relationships yet.", is its own small builder call so the live class can re-render just that inner region after the last row is deleted, without a second storage round-trip). Each row's Delete button uses the `ApplicationV2` `actions` map's per-element `(event, target)` form — a genuinely new-to-this-codebase API surface, researched against Foundry's official API docs (`ApplicationClickAction`: `(event: PointerEvent, target: HTMLElement) => void | Promise<void>`, `this`-bound to the Application instance) rather than assumed, since `relationship-authoring-window.ts`'s own single Save button never needed to disambiguate between multiple elements sharing one `data-action` value; `target` is the clicked button, and the row's id is resolved via `target.closest('[data-relationship-id]')` off the enclosing `<li>`. Confirm-before-delete uses `foundry.applications.api.DialogV2.confirm(...)` — also previously unexercised (only `.prompt` had been used, ADR-0009's Amendment) — researched the same way: resolves `true` on an explicit "Yes," `false` on "No," `null` if dismissed; this code treats anything other than exactly `true` as a cancel. On confirm, `storage.deleteRelationship(id)` is called and just that row is removed from the DOM (`row.remove()`) — the window itself never closes, matching ADR-0013's named "batch cleanup in one sitting" requirement. `deleteRelationship`/`getRelationshipsForNode` needed zero `StorageProvider` change, confirmed via grep before writing anything (both already existed end-to-end: the port, `sqlite-storage-provider.ts`, the Worker RPC transport, and existing tests). Wired into `module-entry.ts` as a third registration alongside the existing two. `header-controls-coexistence.test.ts` extended from n=2 to n=3 on both hook names (`getHeaderControlsJournalEntryPageSheet` and, newly, `getHeaderControlsActorSheetV2`), asserting all three listeners fire and all three push into one shared `controls` array — a real, named ADR-0013 requirement, not optional polish. 23 new unit tests in `relationship-list-window.test.ts` (using plain fake objects satisfying the `MinimalDomElementLike` structural interface to exercise the delete-confirm-remove flow directly, the same "test what's pure/testable, flag what needs a live render" discipline every prior file in this family has used); 324 unit tests total (300 pre-existing + 23 new + the 1 net-new coexistence test replacing the old n=2 assertion). One judgment call, flagged: when the clicked document's Node can't be resolved at all (an unsupported document type reaches the entry point), the window doesn't open at all rather than opening scoped to nothing — a stricter behavior than the authoring window's own prefill-optional handling, since a list window has no meaningful "no node" state to render. `npm install`, `tsc --noEmit`, `npx eslint .`, `npx vitest run` all clean, no regressions. Committed on its feature branch, not merged to `dev`, not pushed — needs the same live Foundry verification (both new API surfaces, plus the third header-control button's real coexistence) every prior `getHeaderControls*` addition in this lineage has needed before Alberto trusts it. **VIEW-001 is now decided too (2026-09-08, `decisions/ADR-0014-graph-view-sidebar-tab.md`, item 24 below):** a first-level sidebar tab (native `CONFIG.ui.sidebar.TABS`, no third-party registration dependency) rendering Cytoscape.js over CORE-005's presets, plus `View`'s first real Core shape (new ticket, CORE-006, since View has been decided since ADR-0005 but never implemented). Split into CORE-006 (Core+Storage), VIEW-001a (tab shell + overview + Direct-only rendering), and VIEW-001b (clustering + Curated-by-me + saved layout, needs CORE-006). **CORE-006 (issue #55) is now implemented (2026-09-08, on `feat/core-006-view-storage`, not yet merged):** `src/core/domain/view.ts` gives `View` its first real shape — `View extends KnowledgeElement` (`kind: 'view'`, `format: 'graph'` as `VIEW_FORMATS`, a one-value closed union), `spec: GraphViewSpec` as the ADR's discriminated union (derived presets store only preset + `rootNodeId`; `curated-by-me` adds a de-duplicated `relationshipIds` list and an optional `layout` of finite `{x, y}` positions), with `createView`/`isView`/`isGraphViewSpec` in the same validating-factory style as `createNode`/`createRelationship`. `GRAPH_VIEW_PRESETS` is a by-convention copy of CORE-005's `TRAVERSAL_PRESETS` (kept out of a `core/domain` → `core/query` dependency, same non-dependency `relationship-definition.ts` keeps). `StorageProvider` gained `saveView`/`getView`/`deleteView`/`listViews`, implemented in `SqliteStorageProvider` against a new SQLite **migration 2** `views` table (JSON-encoded `spec` column, `CHECK (format IN ('graph'))`, deliberately **no** FK to `nodes`/`relationships` — same no-cascade/no-delete-blocking reasoning as `relationships`, ADR-0014 point 7 / ADR-0007 point 8), plus `row-mapping.ts` `viewToRow`/`rowToView` and the Worker RPC (`protocol.ts` method union, `worker-storage-provider.ts` proxy methods; `rpc-dispatcher.ts` needed nothing, it's generic). Deliberately out of scope: `to-portable-snapshot.ts` still doesn't gather Views (its header already frames that as a separate follow-up — the export DTO carries a placeholder `views: []`), and no Foundry/Adapter code is touched (VIEW-001a/b own that). 35 new unit tests (359 total, up from 324); `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean in-sandbox. Committed on its feature branch, not merged/pushed. VIEW-001b (issue #57) is now unblocked. **VIEW-001a (issue #56) is now implemented (2026-09-08, on `feat/view-001a-codex-sidebar-tab`, not merged):** the Codex — a first-level Foundry sidebar tab rendering the whole Knowledge-Element graph with **Cytoscape.js** (added as `cytoscape ^3.34.3`, the new runtime dependency ADR-0014 point 3 accepts). `src/adapters/foundry/graph-view-elements.ts` (new, pure, fully unit-tested) is the Node/Relationship → Cytoscape `{group,data}` transform — dangling edges excluded the same way `resolveTraversal` drops them (ADR-0007 point 8); `buildGraphViewElementsFromTraversal` feeds the same transform from a CORE-005 `TraversalResult`. `src/adapters/foundry/codex-sidebar-tab.ts` (new) holds the `CONFIG.ui.sidebar.TABS` registration (`registerCodexSidebarTab` — testable against a fake `CONFIG`, and tested) plus the `AbstractSidebarTab` subclass (deferred-class pattern like `relationship-list-window.ts`): whole-graph overview on open, single-tap a Node → its "Direct only" 1-hop neighbourhood via `resolveTraversal`, double-tap → open its real Foundry sheet (`foundry.utils.fromUuid` + `sheet.render(true)`), plus a "Whole graph" reset button. `foundry-globals.d.ts`/`eslint.config.js` gained `CONFIG` + `foundry.applications.sidebar` + `globalThis`. **First live deploy (v14.367) crashed at bundle eval — root cause found and fixed:** Foundry v13+ locks `Array.prototype.equals` (+ `deepFlatten`/`filterJoin`/`findSplice`/`partition`) non-writable + non-configurable, and Cytoscape's collection prototype (`Object.create(Array.prototype)`) `Object.assign`s its own `equals` onto it → an uncatchable `TypeError` that killed the whole module at load. Cytoscape is now loaded via a **guarded dynamic `import()`** (`ensureCytoscape`) that swaps in a defensive `Object.assign` (falls back to `defineProperty` where `[[Set]]` would throw) only for the duration of Cytoscape's evaluation — which also code-splits it into its own `cytoscape.esm-*.js` chunk (~639KB / 168KB gzip), keeping `archivexus.js` at ~45KB. The live client also confirmed the real `SidebarTabDescriptor` shape (`{ tooltip, icon }`, modelled on the core `settings` tab — the earlier `{ active, icon, tooltip }` guess dropped `active`) and that `AbstractSidebarTab` has no Handlebars mixin (raw `_renderHTML`/`_replaceHTML` is correct). Two follow-up fixes on the live client: a `Hooks.callAll('archivexus.ready')` from `module-entry.ts` so the tab re-pulls the graph once STORE-003's storage is up (it renders before `ready`), and holding the graph in `#pendingElements` so its layout only runs while the tab is the *visible* one (Cytoscape lays out against container pixels; a hidden 0×0 tab piled all 102 nodes at the origin). **Verified end-to-end on v14.367:** auto-loads 102 nodes in a grid on open, tap → traversal view, right-click → opens the real Foundry sheet, "Whole graph" reset works, no console errors. **Post-review addendum + ADR-0014 Amendment (2026-09-08, same branch):** (a) fixed a visibility leak — a player saw a GM-only Node; the Codex now filters `hidden` Nodes for non-GM viewers per ADR-0003 (pure `filterNodesForViewer`, conservative — filters on `Node.visibility` from default ownership; per-user resolution is deferred, see the Amendment's A4/A8); (b) a ux-ui-designer pass worked through Alberto's VIEW-001b redesign, formalized as **`decisions/ADR-0014-graph-view-sidebar-tab.md`'s Amendment (2026-09-08)** — the Cytoscape canvas moves out of the sidebar into a **standalone singleton `ApplicationV2` popout window**; the sidebar tab **becomes a node navigator** (search + type-grouped list, no graph); **favourites** = a pinned navigator group backed by `game.user` flags, explicitly not Core state; gestures single-tap = select + a new **Inspector panel** (renders a Node's attached Blocks as clickable links — Alberto confirmed his "double-tap → related journals/actors" meant *attached content*, not more graph), double-tap = open sheet, right-click = context menu; visibility gets no player-facing "something hidden" placeholder + a GM-only "preview as player" toggle; **CORE-006 stands unchanged**. Revised ticket split VIEW-001b (popout + Inspector) → VIEW-001c (navigator) → VIEW-001d (favourites) → VIEW-001e (clusters) → VIEW-001f (curated + saved layout). (c) VIEW-001a's own gestures trimmed in the same commit to a clean subset: single-tap = "Direct only", double-tap = open sheet (the rest is VIEW-001b). Known cosmetic (non-blocking, disappears with the popout): grid node labels overlap in the narrow sidebar. 380 unit tests; `tsc --noEmit`/`eslint`/`vitest run`/`build:foundry-module` all clean. Committed on its feature branch, not merged/pushed. Architect follow-ups from the Amendment (A8): favourites-as-user-flags light confirm, per-user visibility resolution scope, whole-graph-as-a-saved-View (conditional), `RelationshipDefinition` persistence as a VIEW-001e pre-req.

## Target users

Tabletop RPG Game Masters running campaigns in Foundry VTT who want a connected memory of their world (characters, locations, organizations, events, relationships, history) instead of fragmented, hard-to-navigate documents.

## Tech stack

Planned, per `README.md`: TypeScript (core language), Foundry VTT API (primary platform integration), Vite (dev tooling), Vitest (unit testing), ESLint, Prettier. Architecture is intentionally platform-agnostic at the Core (see `01_ARCHITECTURE.md`); Foundry is the first Adapter, not a Core dependency.

## Constraints

Installing, updating, enabling or disabling a module inside a live Foundry world is always a manual action performed by the user (GM) — never by an agent or this session's automation (ADR-0006). Otherwise none formally documented yet; worth capturing here once more appear (time budget, whether this ships as a paid Foundry module, compatibility requirements with specific Foundry versions, etc.).

## Current priorities (next 2-4 weeks)

_Previous priorities #1 and #2 (closing the docs consistency review findings, resolving `03_DOMAIN_MODEL.md`'s Outstanding Questions) are done as of 2026-08-27 — see the per-concept Decisions sections in `03_DOMAIN_MODEL.md` and ADR-0005._

1. ~~Implement the Knowledge Element base abstraction~~ — done, merged to `dev` (CORE-001, board card moved to Done, issue #8 auto-closed).
2. ~~Implement Node~~ — done and merged to `dev` (`src/core/domain/node.ts`, CORE-002). Reviewed (docs-consistency findings fixed across three rounds) and merged; board card moved to Done, issue #9 auto-closed.
3. ~~Implement the Foundry Adapter's `JournalEntryPage` → Node mapping~~ — done and merged to `dev` (`src/adapters/foundry/journal-entry-page-to-node.ts`, ADAPT-001, board card moved to Done, issue #10 auto-closed). Validated against live Foundry data (5 real pages created in the user's "Dev test" world, `journal-entry-page-to-node.live-fixtures.test.ts`) — confirmed Foundry's real `-1` ownership-inheritance sentinel matches what the code assumed. Still doesn't map page content to Blocks (`Block`'s shape is still a placeholder) or resolve that inheritance sentinel against the parent `JournalEntry` — both flagged as open follow-ups, not yet their own tickets.
4. ~~Foundry module scaffold~~ — done, merged to `dev` (`module.json`, `module-entry.ts`, `build:foundry-module`, ADAPT-002, board card moved to Done, issue #19 auto-closed). Built, released and manually installed by the user; confirmed loading in a live Foundry v14 GM session at `localhost:30000`.
5. ~~Foundry Adapter — map `Actor` to Node~~ — done, `src/adapters/foundry/actor-to-node.ts` (ADAPT-004, issue #22). `mapActorToNode`, same pattern as ADAPT-001: id from uuid, title from name, type from an explicit `flags.archivexus.nodeType` flag falling back to `Character`, visibility from `ownership.default`. Deliberately does not branch on Foundry's own `actor.type` (character/npc/encounter/group, confirmed from the real "Academia El Último Norte" campaign's 41 Actors) — product-owner call on issue #22, consistent with ADAPT-001's no-content-inference rule. Validated against 4 real Actors, one per real `actor.type`.
6. ~~Fix Node title collisions on generic Foundry page names~~ — done (#25). Real campaign data showed 5 distinct NPCs' "Retrato" (Portrait) pages collapsing to the same Node title. `mapJournalEntryPageToNode` now qualifies the title with the parent JournalEntry's name when it disambiguates anything (`FoundryJournalEntryPageLike.parent.name`), falling back to the bare page name otherwise — fully backward compatible.
7. ~~Decide how Foundry `Scene`s map into Archivexus~~ — decided (ADAPT-005, issue #23, 2026-08-31): a Scene is never its own Node (same reasoning as `JournalEntry`) — it's a `scene`-type Block on whatever Node the GM explicitly links it to, and stays unmapped (not force-created as a placeholder Node) if the GM hasn't linked it yet. This also settled Block's real shape for the first time (`{ type, uuid, title? }`, always a Foundry-element reference — see `03_DOMAIN_MODEL.md`'s Knowledge Element Decisions), replacing `Block.data: unknown`'s placeholder. Implementing the mapping itself (`mapSceneToBlock` or similar, plus updating `Block`'s real TS type in `src/core/domain/block.ts`) is separate, not-yet-scoped follow-up work — this ticket was decide-only.
8. ~~Foundry UI extension/customization~~ (ADAPT-003, issue #20) — done, live-verified (`src/adapters/foundry/actor-node-type-tag.ts`, 2026-08-31, `decisions/ADR-0009-foundry-ui-extension-hooks.md`). Went through two implementations: first, a permanent tagging bar injected into every Actor sheet via `renderActorSheetV2` — built, found and fixed a real DOM-overlap bug via live testing (the control overlapped Foundry's floating title bar; fixed by inserting after `.window-header` instead of inside `.window-content`, ADR-0009's Correction), then fully verified end-to-end (a typed value saves to `actor.flags.archivexus.nodeType` via Foundry's native form handling). Alberto then challenged it on two fronts — its position, and a more fundamental "is this even necessary?" — and a grep confirmed nothing in Core reads `Node.type` yet, so the permanent bar had zero current visible payoff for the common case (a Player Character already gets the right type for free from `ACTOR_FALLBACK_NODE_TYPE`). Redesigned per Alberto's choice into an **opt-in header-control button**: `getHeaderControlsActorSheetV2` pushes a button into the sheet's header-controls dropdown, which opens a `DialogV2.prompt` dialog (`buildNodeTypeDialogContent`, pure/unit-tested) only when a GM actively wants to reclassify something — no more persistent DOM injection at all. See ADR-0009's Amendment for the full reasoning and live-verification notes (including the "header controls aren't recomputed by a plain re-render, only a close+reopen" gotcha). A reviewer pass then caught two stale docs: ADR-0009's own Decision section still read as if the superseded mechanism were current (fixed with inline pointer notes on points 2 and 4 — a first pass at the point-2 note accidentally truncated real, still-accurate scoping context while claiming to preserve it, caught in a second self-review pass and restored in full), and `README.md`'s architecture diagram still showed Storage as an undecided candidate list from before STORE-001 (fixed to show the decided engine and point at `01_ARCHITECTURE.md`). **Merged to `dev`** (2026-08-31, issue #20). Scoped to Actor first, matching every prior Adapter ticket's incremental-per-document-type shape; JournalEntry/Scene tagging UI is separate, not-yet-scoped follow-up work — **narrowed and settled for JournalEntryPage specifically by ADR-0011 (item 17 below): the same control extended with an "attach to an existing Node" option, not a wholly new mechanism.**
9. ~~Relationship~~ (Core domain, per ADR-0005) — done, merged to `dev` (CORE-003, 2026-08-31): `src/core/domain/relationship.ts` implements the Relationship instance shape (`origin`/`target` Node ids, `definitionId`, composed from KnowledgeElement exactly like Node), mirroring CORE-002's pattern, with unit tests (`relationship.test.ts`, including rejecting self-relationships — `03_DOMAIN_MODEL.md`'s "Can a Relationship connect a Node to itself?" Decision, added during reviewer pass) and exported from `src/core/domain/index.ts`. Deliberately scoped to the instance only — Relationship Definition itself (inverse/cardinality/symmetry/validation/traversalCategory as real, versioned Core state per ADR-0007) is separate, not-yet-scoped follow-up work; `definitionId` is just a string reference for now. Then View, lower priority still since it depends on having enough Nodes/Relationships worth projecting; its own MVP scope (3 fixed traversal presets, no open config) is settled by ADR-0007.
10. ~~Decide Archivexus's Storage Provider~~ — done (STORE-001, issue #32, 2026-08-31): `decisions/ADR-0008-storage-provider.md` settled on embedded SQLite (WASM, `opfs-sahpool` VFS) as the primary store, satisfying ADR-0007's 1-hop-lookup and no-cascading-FK constraints via real indexes, plus a deliberate user-triggered export (a generated JSON snapshot, not a raw copy of the SQLite file) as the reachability mechanism for a future standalone frontend/external AI agent — no live sync, read-only for now. `01_ARCHITECTURE.md`'s Storage section updated to match. The one implementation-risk item ADR-0008 carried forward — whether dedicated-Worker creation and `opfs-sahpool` behave cleanly inside a real running Foundry client — is now resolved: a STORE-002 spike confirmed it works (see ADR-0008's addendum), after finding and fixing a real Vite `base`-path bug along the way (`vite.foundry.config.ts` now uses a relative base).
11. ~~Implement the real Storage Provider~~ — done (STORE-003, issue #38, 2026-09-06). `src/core/storage/storage-provider.ts`: the `StorageProvider` port (Node/Relationship CRUD + `getRelationshipsForNode`, ADR-0007's 1-hop primitive) Core/Adapters program against — storage stays swappable per `01_ARCHITECTURE.md`. `src/storage/sqlite/`: the concrete engine — `migration.ts` (ordered `PRAGMA user_version`-tracked SQL migrations; `nodes`/`relationships` tables, deliberately **no** FK on `relationships.origin`/`target` per ADR-0007/ADR-0008, `CHECK (origin <> target)` and real indexes on `origin`/`target`/`type` instead), `row-mapping.ts` (Node/Relationship ⟷ row, JSON-encoding the not-yet-queried fields), `sqlite-executor.ts` (a minimal structural interface over the real `@sqlite.org/sqlite-wasm` OO1 API, same no-full-library-dependency tradeoff as Foundry's own ambient types), `sqlite-storage-provider.ts` (the actual `StorageProvider` implementation, runs inside the dedicated Worker `opfs-sahpool` requires), and a Worker RPC (`worker/protocol.ts`, `worker/rpc-dispatcher.ts`, `worker-storage-provider.ts`, `worker/sqlite.worker.ts`, `create-sqlite-storage-provider.ts`) bridging the async `StorageProvider` contract across that Worker boundary. `src/core/storage/to-portable-snapshot.ts`: the Core-owned, full/unredacted JSON export ADR-0008 settled on, denormalizing Relationships with inlined origin/target titles and `null` for a dangling endpoint. `src/adapters/foundry/storage-sync.ts` + `export-snapshot.ts` wire the existing `mapActorToNode`/`mapJournalEntryPageToNode` mappings into real persistence and expose a console-triggered export (`game.modules.get('archivexus').api.exportSnapshot()`) via Foundry's own `saveDataToFile`; `module-entry.ts` opens storage on `ready`, backfills existing Actors/Pages, and wires `createActor`/`updateActor`/`createJournalEntryPage`/`updateJournalEntryPage` Hooks — deliberately no delete-Hook wiring, since Node deletion policy is still an open question (ADR-0007). 149 unit tests, including `sqlite-storage-provider.test.ts`/`migration.test.ts` run against a real (if in-memory, no OPFS) `@sqlite.org/sqlite-wasm` engine via its Node build — real SQL, real schema, real constraints, not a hand-rolled fake. `npm run build:foundry-module` verified in-sandbox: the Worker code-splits correctly, the `base: './'` fix from STORE-002 still resolves the Worker chunk's URL relatively, and the wasm binary is self-contained (inlined as a base64 data URI by the package's own browser build — no separate asset path to worry about). **Reviewer pass (2026-09-06):** one blocking finding, fixed per Alberto's decision — `PortableNode`/`PortableRelationship` were silently dropping `history`/`blocks`/`references` (three of `KnowledgeElement`'s nine fields), contradicting ADR-0008 point 6's "full, unredacted snapshot" and the export's actual purpose (letting an external AI or human agent reconstruct the whole world from it). Now carries all nine fields (`history`'s `Date` timestamps reshaped to ISO strings for JSON-serializability only); the test that had asserted the truncated shape as complete now asserts the real one; new Decision entry in `03_DOMAIN_MODEL.md`'s Knowledge Element Decisions. Three non-blocking findings also fixed: `WorkerStorageProvider.close()` now actually calls `Worker.terminate()` after the `close` RPC acks, instead of only sending the message; a `worker.onerror` handler now rejects `init()` and any other in-flight RPC calls instead of hanging forever if the Worker script fails to load; `vite.foundry.config.ts`'s `worker: { format: 'es' }` comment no longer references the deleted STORE-002 spike file, since it's now permanently required by the real `sqlite.worker.ts`. 152 unit tests (149 + 3 new for the Worker-transport fixes); `tsc --noEmit`, `eslint`, `npm run build:foundry-module` all verified clean in-sandbox. Live Foundry+OPFS re-verification done afterward: a 94KB SQLite file confirmed persisting across a real Foundry reload with clean console logs (`npm install && npm test`/`npx eslint .`/both `npm run build*` targets on Alberto's own machine — see `SESSION_LOG.md`).
12. ~~Relationship Definition as real Core state~~ — done (CORE-004, issue #39, 2026-09-06): `src/core/domain/relationship-definition.ts` implements `RelationshipDefinition` (`inverse`, `cardinality`, `symmetry`, `traversalCategory` — ADR-0007's closed 8-value taxonomy — and an optional `validation` origin/target Node-type allow-list), versioned, following `createNode`/`createRelationship`'s validating-factory style but deliberately _not_ composing `KnowledgeElement` (Definitions are not Knowledge Elements). `cardinality`'s enum and `validation`'s shape are judgment calls, not settled by any ADR/domain-model text — recorded as new `03_DOMAIN_MODEL.md` Decisions under "Definitions" rather than invented silently. Two cross-field invariants enforced (a symmetric Definition's `inverse` must equal its `name`; a symmetric Definition can't use an asymmetric cardinality) — reversible instance-level constraints, same treatment as CORE-003's self-relationship rule, not ADR-level calls. `resolveRelationshipDefinition` is the minimal "resolve by id" query surface this ticket needs (a pure lookup, not a registry or the View Query API). 28 unit tests, exported from `src/core/domain/index.ts`. Deliberately scoped Core-only — no storage persistence, no Foundry-side authoring UI (separate future tickets); `Relationship.definitionId` (CORE-003) is unchanged. Merged to `dev` via PR #41, reviewed and approved (traversalCategory taxonomy checked against ADR-0007, both invariants verified correct and tested in both directions).
13. Scope the path to the visual relationship-authoring/graph-View goal — done (product-owner, 2026-09-06): three new tickets opened. **CORE-005** (issue #42, implement-only) — the Core Query API executing ADR-0007's 3 traversal presets against `StorageProvider`/`resolveRelationshipDefinition`, now unblocked. **ADAPT-006** (issue #43, decide-only) — how a GM authors a Relationship instance between two Nodes in Foundry; explicitly flagged not to assume ADAPT-003's single-document header-button pattern transfers, since picking two arbitrary cross-document-type entities is a different interaction shape. **VIEW-001** (issue #44, decide-only) — the graph View's rendering/interaction design, including ADR-0007's own named open item on content-prominence ordering and cluster grouping/collapse. Next up: ux-ui-designer on ADAPT-006.
14. ~~Decide the Foundry Relationship-authoring UI~~ — decided (ADAPT-006, issue #43, 2026-09-06): `decisions/ADR-0010-relationship-authoring-ui.md`. See Stage paragraph above for the summary. Next: scope and implement the concrete ticket (software-developer) — needs, at minimum, a temporary seeded `RelationshipDefinition[]` list until persistence lands, since none currently exists to populate the Definition picker from.
15. ~~Implement the Core Query API executing ADR-0007's 3 traversal presets~~ — done (CORE-005, issue #42, 2026-09-06): `src/core/query/traversal.ts`/`index.ts`. See Stage paragraph above for the summary, including the one real bug (root re-appearing in depth-2 results via a back-edge) found and fixed while picking up a prior rate-limited attempt. Next up: VIEW-001's design pass, per item 13's sequencing — the ADAPT-006 implementation ticket is now done too (ADAPT-007, item 16 below), so VIEW-001's own implementation has both of CORE-005's and ADAPT-007's real dependencies to build on.
16. ~~Implement the Foundry Relationship-authoring UI~~ — done and merged to `dev` (ADAPT-007, issue #45, 2026-09-06): see Stage paragraph above for the summary and the live-verification results. `src/adapters/foundry/relationship-authoring-window.ts` plus five new pure support modules implement every piece of `decisions/ADR-0010-relationship-authoring-ui.md`'s Decision section. Not yet pushed to `origin/dev`. Follow-up opened: ADAPT-008 (issue #48) — delete a Relationship instance, since this ticket was deliberately create-only.
17. ~~Decide how a JournalEntryPage can attach to another Node instead of always becoming its own~~ — decided (architect, 2026-09-06, `decisions/ADR-0011-journal-entry-page-node-attachment.md`): triggered by Alberto's real "Lord Fausto Farcon" scenario (4 disconnected Foundry documents with no way to unify them). Extends ADR-0009's existing per-document `DialogV2` tagging control (JournalEntryPage sheets specifically, not Actor sheets — an Actor stays always-standalone) with a second, mutually-exclusive field: a `<document-tags single>` picker (reusing ADR-0010's widget) to attach the page to an existing Node instead of picking its own type. Storage shape: `flags.archivexus.attachedToNodeId`, a plain Foundry UUID; resolution/orchestration (whether the target actually exists, deleting an already-synced page's own standalone Node, adding the `{type: 'JournalEntryPage', uuid, title}` Block onto the target, and a non-blocking warning if real Relationships already existed against the retagged page) moves into `storage-sync.ts`'s async layer — `mapJournalEntryPageToNode` itself stays pure and unchanged. No forced migration of the 61 already-backfilled pages; attachment is opt-in and opportunistic only. Judged ux-ui-designer's alternative (`same-entity-as` symmetric RelationshipDefinition) superseded for this specific problem, not kept as a parallel mechanism — see the ADR's Alternatives Considered. `03_DOMAIN_MODEL.md`'s Node Decision amended in place (inline pointer, following ADR-0009's own amendment precedent) rather than silently contradicted. Decide-only — implementing the dialog extension and `storage-sync.ts` orchestration is separate, not-yet-scoped follow-up work; a DBA follow-up pass still needs to settle the idempotent Block-upsert-by-uuid mechanics and the (currently unhandled, explicitly named) detach/re-attach-elsewhere stale-Block-cleanup case.
18. ~~DBA resolution of ADR-0011's two named open mechanics~~ — decided (dba, 2026-09-06, ADR-0011's Amendment). Idempotent Block-upsert-by-uuid: two small pure Core functions, `upsertBlockByUuid`/`removeBlockByUuid` on `src/core/domain/node.ts` (not Foundry-Adapter logic — general Node/Block manipulation ADAPT-005's still-unimplemented Scene→Block mapping will reuse), needing nothing beyond `StorageProvider.getNode`/`saveNode` (both already exist). Detach/re-attach-elsewhere cleanup: the architect's own "full scan over `listNodes()`" is confirmed as the right design, run unconditionally on every `syncJournalEntryPage` call (not gated on whether the attachment flag changed) — justified against Alberto's real ~102-Node count and the fact that `module-entry.ts`'s Hook wiring (`withStorage`'s fire-and-forget `void action(storage).catch(...)`) already never blocks Foundry's UI regardless of scan cost. No new `StorageProvider` method, no schema/migration change — `blocks` stays an unindexed JSON column, purely an orchestration-layer (`storage-sync.ts` + the new `node.ts` helpers) concern. One real, currently-unscoped prerequisite surfaced, not covered by ADR-0011 or ADAPT-005 alone: `src/core/domain/block.ts`'s actual TS shape is still CORE-001's `{ id, type, data }` placeholder, not the `{ type, uuid, title? }` shape both ADR-0011 and `03_DOMAIN_MODEL.md` already assume — whoever implements ADR-0011 must update `block.ts` first or as part of the same ticket (confirmed via repo-wide grep to be an isolated, low-risk change; nothing outside `block.ts` reads `id`/`data`). Decide-only, no code changed by this pass. This closes out ADR-0011's remaining open item — the ADR is now considered fully ready for an implement ticket (software-developer), covering: the `getHeaderControlsJournalEntryPageSheet` dialog extension, the `block.ts` shape update, the two new `node.ts` Core helpers, and `storage-sync.ts`'s attach/detach/cleanup orchestration.
19. ~~Implement ADR-0011: JournalEntryPage-to-Node attachment~~ — done (software-developer, ADAPT-009, issue #49, 2026-09-06). See the Stage paragraph above for the full summary. Branch `feat/adapt-009-journal-entry-page-attachment`, committed but not merged to `dev`/not pushed. Judgment call flagged for Alberto: the tagging dialog's mutual-exclusivity mechanic is two always-enabled fields with save-time precedence (attach wins if resolved), not an interactive radio toggle — matches ADR-0011 point 1's own text and avoids extra untested live-DOM event wiring. Named gap: ADR-0011 point 5's non-blocking orphaned-Relationships warning (`ui.notifications.warn`) was not implemented, since it fell outside the orchestration checklist this ticket was scoped against — a real, decided-but-unbuilt piece of the ADR, worth a fast-follow or an explicit call that it's not worth building. **Closed out by item 20 below.** Needs live Foundry verification (both header-control buttons coexisting on a JournalEntryPage sheet, the attach `<document-tags>` field's drag/paste behavior, and the end-to-end attach/detach flow against real data) before Alberto trusts it, same discipline as every prior `getHeaderControls*` addition.
20. ~~Two ADAPT-009 reviewer follow-ups: the orphaned-Relationships warning, and a real hook-coexistence test~~ — done (software-developer, ADAPT-010, issue #50, 2026-09-06), same branch (`feat/adapt-009-journal-entry-page-attachment`), two additional commits. (1) ADR-0011 point 5's `ui.notifications.warn`: `storage-sync.ts`'s `syncJournalEntryPage` now checks `storage.getRelationshipsForNode(page.uuid)` before deleting a page's own standalone Node in the attach-resolves branch, warning the GM by name/count if any exist — non-blocking, the delete/attach proceeds regardless. `foundry-globals.d.ts`/`eslint.config.js` gained the minimal `ui.notifications.warn` ambient declaration this needed. New test in `storage-sync.test.ts`'s ADR-0011 suite confirms both the warning and the non-blocking behavior. (2) The claim that `journal-entry-page-node-tag.ts`'s and `relationship-authoring-window.ts`'s `getHeaderControlsJournalEntryPageSheet` registrations coexist — asserted in code comments and this file's own Stage paragraph, but each module's test suite previously only exercised its own registration against its own isolated mock `Hooks` — is now actually tested together: a new `header-controls-coexistence.test.ts` wires both real registration functions against one shared mock `Hooks` (an array of listeners per hook name, matching real Foundry dispatch) and fires the hook once with a shared `controls` array. **Confirmed correct, not a bug**: both `Hooks.on` calls register independently (2 listeners, not 1 replacing the other), and both modules' entries land in the same `controls` array. 2 new unit tests (300 total); `npm install`, `tsc --noEmit`, `npx eslint .`, `npx vitest run` all clean, no regressions.
21. ~~Formalize the interim CORE-005 read surface, updated for ADR-0011~~ — decided (ux-ui-designer, 2026-09-06, `decisions/ADR-0012-node-connections-panel.md`). Confirmed via grep that CORE-005 (`src/core/query/traversal.ts`) still has zero real UI consumers anywhere in `src/adapters/foundry/`. Formalizes an earlier, never-written-up brainstorm answer — a narrow "Direct only" connections panel, entered via the same `getHeaderControls*` pattern as ADAPT-003/006/009 — and updates it for ADR-0011's existence: the earlier design's whole "collapse `same-entity-as` clusters into one row with facet tabs" plan is retracted outright, not simplified, since ADR-0011's page-attachment mechanism already deduplicates any page a GM has explicitly attached (it's a Block on the target Node, never a separate row) with zero clustering logic needed; the only residual case (an unattached page that already has a Relationship authored against it) shows as an ordinary extra row, no special handling. Finalized shape: a new `ApplicationV2` window (following `relationship-authoring-window.ts`'s precedent, not `DialogV2`'s), showing only the "Direct only" preset (both "Everything connected" and "Curated by me" deferred to VIEW-001), grouped by `traversalCategory`, sorted degree-descending/alphabetical-tiebreak within each group (a real, scoped answer to ADR-0007 point 6's open content-prominence question — no persisted manual reorder here, that's VIEW-001's job), a no-Actor-yet badge derived from the connected Node's own id prefix (`Actor.` vs. not), and row-click behavior that opens the connected Node's real Foundry sheet plus, new given ADR-0011, an inline clickable list of that Node's attached Blocks (already present on every `Node` `resolveTraversal` returns — no new Core/Storage work). Needs nothing new from `StorageProvider`/CORE-005: `TraversalResult` and the existing `getRelationshipsForNode` cover every requirement named. Proposed ticket: **ADAPT-011** — "Node Connections panel: interim Direct-only read surface for CORE-005" (Alberto to open the actual issue). Decide-only — no code written; `03_DOMAIN_MODEL.md`'s View Decisions/Open Questions and Node/Outstanding-Questions sections updated with pointers.
22. ~~Decide how a GM deletes a Relationship instance~~ — decided (ux-ui-designer, 2026-09-06, `decisions/ADR-0013-relationship-deletion-ui.md`). A third `getHeaderControls*` button, "Relationships…", opens a new dedicated `ApplicationV2` list window (`relationship-list-window.ts`) — not a reuse of ADAPT-007's create-oriented authoring window, which has no way to disambiguate multiple Relationships between the same pair or recall which Definition was used. Lists every Relationship via `getRelationshipsForNode` (1-hop, already exists), rendering each row using the Relationship's own already-persisted `title` (Rule 4 — no new endpoint-resolution logic, and a dangling-endpoint Relationship still renders sensibly for free). Delete is gated by a single native `DialogV2.confirm` — a deliberate, explicitly-reasoned exception to this project's warn-never-block posture (ADR-0010 point 6, ADR-0011 point 5), justified because an actual delete has no recovery path, unlike those cases. The window stays open after each delete (removing just that row), matching the real use case: cleaning up several mistaken/test Relationships in one sitting. Confirmed deleting a Relationship never affects either connected Node (no FK, no cascade) and needs zero `StorageProvider` change — `deleteRelationship` was already fully implemented end-to-end (port, SQLite, Worker RPC, tests) before this ticket. Decide-only; implementation (the new file, its header-control registrations, and a coexistence test extended to the now-three independent listeners sharing the same two hook names) is separate, not-yet-scoped follow-up work.
23. ~~Implement ADR-0013: the Relationship deletion UI~~ — done (software-developer, ADAPT-012, issue #53, 2026-09-06). See the Stage paragraph above for the full summary. Branch `feat/adapt-012-relationship-deletion-ui`, committed but not merged to `dev`/not pushed. Two previously-unexercised Foundry API surfaces this ticket needed (the `ApplicationV2` `actions` map's per-element `(event, target)` form, and `DialogV2.confirm`) were both researched against Foundry's official API docs rather than assumed — see the Stage paragraph for the exact confirmed shapes. Judgment call flagged for Alberto: an unresolvable clicked-document type means the window simply doesn't open (a stricter behavior than the authoring window's own optional-prefill handling). Needs live Foundry verification (both new API surfaces, plus the third header-control button's real coexistence on both sheet families) before Alberto trusts it, same discipline as every prior `getHeaderControls*` addition in this lineage.
24. ~~Decide VIEW-001: the graph View's rendering/interaction design~~ — decided (2026-09-08, `decisions/ADR-0014-graph-view-sidebar-tab.md`), prompted by Alberto asking for it framed as "a section at the same level as Journal, Scenes, Actors." A first-level Foundry sidebar tab (not a `getHeaderControls*` panel, not a floating window), registered via Foundry's native `CONFIG.ui.sidebar.TABS` (no third-party sidebar-registration dependency, matching ADR-0009's posture) rendering **Cytoscape.js** (accepted as a new runtime dependency — no native alternative exists, unlike ADR-0009's UI-extension case; direct precedent in the community module `foundryvtt-mindmap`). With no Node selected it shows the whole current graph, not one of ADR-0007's three presets (all three assume a queried root Node already exists); clicking a Node then applies those existing presets via CORE-005. Coexists with, doesn't retire, ADR-0012's Connections panel (different real use cases: in-flow single-Node review vs. whole-campaign spatial exploration). Also decides `View`'s first real Core shape and `StorageProvider` CRUD for it (new ticket, **CORE-006** — View has been decided since ADR-0005 but never implemented). Proposed tickets (Alberto to open): **CORE-006** (View + Storage, Core-only), **VIEW-001a** (sidebar tab shell + whole-graph overview + "Direct only" rendering, needs only CORE-005), **VIEW-001b** ("Everything connected" clustering + "Curated by me" + saved layout, needs CORE-006). `03_DOMAIN_MODEL.md`'s View Decisions/Open Questions updated with pointers. Decide-only — no code written.
25. ~~Implement CORE-006: `View` as Core state + `StorageProvider` View CRUD~~ — done (software-developer, issue #55, 2026-09-08, on `feat/core-006-view-storage`, merged to `dev`). See the Stage paragraph above for the full summary. Pure Core + Storage, zero Foundry code (VIEW-001a/b own the sidebar tab and Cytoscape rendering). `src/core/domain/view.ts` (new), `StorageProvider` + `SqliteStorageProvider` + SQLite migration 2 (`views` table, no FK), `row-mapping.ts`, Worker RPC protocol/proxy. `GRAPH_VIEW_PRESETS` deliberately mirrors CORE-005's `TRAVERSAL_PRESETS` by convention rather than importing it (no `core/domain` → `core/query` dependency). 35 new unit tests (359 total); `tsc`/`eslint`/`vitest`/`build:foundry-module` all clean. Judgment call, flagged: a derived-preset `spec` carrying `curated-by-me`-only fields (`relationshipIds`/`layout`) is rejected by `createView`, not silently dropped — the discriminated union already implies it, mirroring `relationship-definition.ts`'s cross-field checks. `to-portable-snapshot.ts` still doesn't export Views (its own header frames that as a separate follow-up). Unblocks VIEW-001b (issue #57).
26. ~~Implement VIEW-001a: the Codex sidebar tab + whole-graph overview + Direct-only rendering~~ — done (software-developer, issue #56, 2026-09-08, on `feat/view-001a-codex-sidebar-tab`, merged to `dev`). See the Stage paragraph above for the full summary. Adds `cytoscape ^3.34.3` (ADR-0014 point 3's accepted new dependency), loaded via a **guarded dynamic `import()`** after the first live deploy crashed on Foundry's non-writable `Array.prototype.equals` vs. Cytoscape's `Object.create(Array.prototype)` collection prototype (see Stage paragraph / session log). Two new files: `graph-view-elements.ts` (pure Node/Relationship → Cytoscape-element transform, dangling edges excluded per ADR-0007 point 8 — fully unit-tested) and `codex-sidebar-tab.ts` (the native `CONFIG.ui.sidebar.TABS` registration + the `AbstractSidebarTab` subclass). Whole-graph overview on open; single-tap a Node → "Direct only" via `resolveTraversal` (CORE-005), double-tap → open its real Foundry sheet; "Whole graph" reset button. Non-GM viewers only see non-`hidden` Nodes (ADR-0003, pure `filterNodesForViewer` — fixed a leak Alberto found testing with a player account; the "N hidden" hint is GM-only). 380 unit tests (up from 359). `tsc`/`eslint`/`vitest`/`build:foundry-module` all clean. **Verified end-to-end on a live v14.367 client** after follow-up fixes (the `Array.prototype.equals` crash; an `archivexus.ready` hook so the tab loads after storage; gating the layout on tab visibility so 102 nodes don't pile at the origin; the visibility filter). **VIEW-001b redesign is now decided** — `decisions/ADR-0014-graph-view-sidebar-tab.md`'s Amendment (2026-09-08): graph → popout window, sidebar → node navigator, favourites via `game.user` flags, Inspector panel for attached content, GM "preview as player" toggle, revised VIEW-001b–f ticket split. Out of scope for VIEW-001a (→ VIEW-001b–f): the popout, the navigator, favourites, the Inspector panel, clustering, "Curated by me", saved layout, `View`-instance persistence. **VIEW-001b (issue #57) is now implemented (2026-09-08, on `feat/view-001b-graph-popout`, not merged):** the graph popout window + Inspector panel — `graph-popout-window.ts` (a standalone singleton `ApplicationV2`, launched from the sidebar's new "Open graph ⧉" button, with a preset/layout/preview-as-player toolbar and Amendment-A3 gestures), `node-connections.ts` (pure, reusable ADR-0012 points 4–7 connection-list logic), and `cytoscape-loader.ts` / `foundry-viewer.ts` extracted from VIEW-001a for reuse. 20 new tests (400 total). **Live-verified on v14.367 (GM):** popout opens/singleton/whole-graph render, layout picker, single-tap → highlight + Inspector, double-tap → Actor sheet, right-click → overlay menu + "Re-root here", "Preview as player" → 3 nodes + banner, no console errors. Untested (0 authored Relationships): the Inspector's connections list + "Everything" preset. VIEW-001c (sidebar → navigator), VIEW-001d (favourites), VIEW-001e (clusters), VIEW-001f (curated + saved layout) still unstarted. **Relationship Definition persistence is now done (2026-09-08, on `feat/relationship-definition-persistence`, not merged) — CORE-004's deferred fast-follow:** Definitions are real, editable `StorageProvider` state (`save`/`get`/`delete`/`list`, SQLite **migration 3**'s `relationship_definitions` table, Worker RPC), replacing the temporary `SEEDED_RELATIONSHIP_DEFINITIONS` hack (deleted). `src/core/domain/relationship-definitions-default.ts` holds `DEFAULT_RELATIONSHIP_DEFINITIONS` — ~17 starter definitions covering all 8 `traversalCategory` values (up from 3) — seeded into an empty store on `ready` (`bootstrapRelationshipDefinitions`, idempotent). The authoring window and the graph popout's Inspector now read Definitions from storage. Still deliberately open: a GM UI to author/edit Definitions (data layer + console `api` escape hatch only), and Definition version migration. 17 new tests (412 total). **Flag for Alberto:** review/adjust the default list. This also **unblocks VIEW-001e**'s cluster-category-label side.
27. ~~Decide the VIEW-001b redesign~~ — decided (ux-ui-designer, 2026-09-08, `decisions/ADR-0014-graph-view-sidebar-tab.md`'s **Amendment**), prompted by Alberto's live review of VIEW-001a: the ~300px sidebar is too cramped for a real graph. Decision (8 points, A1–A8): the Cytoscape canvas moves into a **standalone singleton `ApplicationV2` popout window** (reusing the `relationship-list-window.ts` deferred-class pattern), launched from the sidebar; the **sidebar tab becomes a node navigator** (search + `node.type`-grouped collapsible list + "Open whole graph"); **favourites** = a pinned navigator group backed by `game.user.setFlag('archivexus', 'favouriteNodeIds', …)` — per-user, explicitly *not* a Knowledge Element / `View` / `StorageProvider` state / in the portable snapshot (a deliberate contrast with a saved "Curated by me" View); **gesture map** single-tap = select + a new **Inspector panel** (renders a Node's attached Blocks as clickable links + its connections list reusing ADR-0012's grouping — Alberto confirmed "double-tap → related journals/actors" meant *attached content*, not a 2-hop expansion), double-tap = open sheet, right-click = context menu; **visibility** — no player-facing "something hidden here" placeholder (leaks metadata), edges already drop when an endpoint is hidden (stated intentional), a GM-only "preview as player (approximate)" toggle; **content-prominence** — canvas spatial layout is enough, no paging on cluster expand; **CORE-006 stands unchanged** (one named gap: the whole-graph overview isn't representable as a `GraphViewSpec` — matters only if Alberto wants to *save* it; conditional architect+DBA follow-up). Revised ticket split (Alberto to open the issues): **VIEW-001b** (popout + Inspector), **VIEW-001c** (sidebar navigator), **VIEW-001d** (favourites), **VIEW-001e** ("Everything connected" clusters), **VIEW-001f** ("Curated by me" + saved layout + ADR-0012 "See in graph" link). Architect follow-ups (A8): favourites-as-user-flags light confirm; per-user visibility resolution scope; whole-graph-as-View (conditional); `RelationshipDefinition` persistence as a VIEW-001e pre-req. Decide-only — VIEW-001a's gestures were trimmed to a clean subset in the same commit, nothing else built.
28. ~~Implement VIEW-001b: the graph popout window + Inspector panel~~ — done (software-developer, issue #57, 2026-09-08, on `feat/view-001b-graph-popout`, merged to `dev`). `src/adapters/foundry/graph-popout-window.ts` (new) — a standalone, resizable, **singleton** `ApplicationV2` window rendering the campaign graph with Cytoscape.js, launched from VIEW-001a's sidebar tab via a new "Open graph ⧉" button (`openGraphPopout` focuses/re-roots the one open window). Toolbar: "Whole graph", preset segmented control (Direct / Everything / Curated-disabled), layout picker, GM-only "Preview as player (approximate)" toggle + banner. Gestures per Amendment A3: single-tap = select + Inspector, double-tap = open sheet, right-click = context menu (Open sheet / Re-root here / Everything connected here). **Inspector panel** (A3a): a selected Node's title/type, its **Attached** Blocks as clickable links, and its **Connections** grouped by `traversalCategory`. `src/adapters/foundry/node-connections.ts` (new, pure, fully unit-tested) implements ADR-0012 points 4–7's connection-list logic (direction-correct labels, taxonomy grouping + "Other" last, degree-desc/alpha ordering) — built reusable for the still-unbuilt ADAPT-011 panel; uses the temporary `SEEDED_RELATIONSHIP_DEFINITIONS` until `RelationshipDefinition` persistence lands. `src/adapters/foundry/cytoscape-loader.ts` + `foundry-viewer.ts` (new) extract VIEW-001a's guarded Cytoscape loader and the `isViewerGM()` glue so both surfaces share them. Non-GM viewers only see non-`hidden` Nodes on every path. 20 new unit tests (400 total, up from 380); `tsc`/`eslint`/`vitest`/`build:foundry-module` all clean. **Live-verified on v14.367 (GM)** — popout opens, singleton, whole-graph render, layout picker, single-tap → highlight + Inspector, double-tap → Actor sheet, right-click → overlay context menu + "Re-root here", "Preview as player" → 3 nodes + banner, no console errors. Untested (0 authored Relationships): the Inspector's connections list and "Everything connected". Out of scope (still): VIEW-001c (sidebar → navigator), VIEW-001d (favourites), VIEW-001e ("Everything connected" collapsed clusters), VIEW-001f ("Curated by me" + saved layout).
29. ~~Implement Relationship Definition persistence~~ — done (software-developer, 2026-09-08, on `feat/relationship-definition-persistence`, merged to `dev`). CORE-004's deliberately-deferred fast-follow. `StorageProvider` gains `save`/`get`/`delete`/`listRelationshipDefinitions`, backed by SQLite **migration 3**'s `relationship_definitions` table (real columns + `CHECK`s, JSON `validation`, PK `id`, no FK) + Worker RPC. `src/core/domain/relationship-definitions-default.ts` (new) holds `DEFAULT_RELATIONSHIP_DEFINITIONS` — ~17 starter definitions across all 8 `traversalCategory` values (was 3, hardcoded in the Adapter). `bootstrapRelationshipDefinitions` seeds an empty store on `ready`, idempotent. The authoring window + graph-popout Inspector read from storage now; `relationship-definitions-seed.ts` deleted. `module-entry.ts` exposes `list`/`saveRelationshipDefinition` on the module `api`. **Still deliberately open:** a GM UI to author/edit Definitions (data layer + console escape hatch only), and Definition version migration (`03_DOMAIN_MODEL.md`'s Open Question — an existing store is never re-seeded). 17 new tests (412 total). No ADR (CORE-004 fast-follow, in `03_DOMAIN_MODEL.md`'s Definitions Decisions). **Flag for Alberto:** review/adjust the default list, its labels and cardinality/validation choices. Not yet live-verified (a real OPFS `user_version` 2 → 3 upgrade + bootstrap on a running client).

### Next batch — from the 2026-09-08 product-owner + ux-ui-designer consult on Alberto's "improve the UI look / onboarding / relationship-management console" asks

Live diagnosis behind these: the per-sheet entry points (node typing, "New Relationship…", "Relationships…") **aren't broken** — they're buried in each sheet's "⋯ Toggle Controls" header dropdown, two clicks deep, which is why Alberto has *"no clear way to setup the whole world"*. The fix is a visible world-level surface + onboarding that points at what exists, not new plumbing.

30. ~~**VIEW-001c — Codex sidebar navigator**~~ — done (software-developer, issue #63, 2026-09-08, on `feat/view-001c-navigator`, not merged). Cytoscape stripped from the sidebar tab; it's now a search box + a `node.type`-grouped node list (`node-navigator.ts`, new, pure — group order: place-like `City`/`Kingdom` → `Character` → `Organization` → rest of `KNOWN_NODE_TYPES` alpha → GM-invented types alpha; alpha by title within a group), each row calling `openGraphPopout(…, { rootNodeId })`; an "Open graph ⧉" toolbar button opens the whole graph. Search filters client-side (row/group `hidden` toggle off a `data-title` attr — no re-render, keeps input focus). Stuck "Storage not ready" state fixed (shows "Loading campaign…" then fills in on the existing `archivexus.ready` hook). `filterNodesForViewer` (ADR-0003) on every load path; "· N hidden" GM-only. +11 unit tests (423 total); `tsc`/`eslint`/`vitest`/`build:foundry-module` clean. **Scope trimmed:** the `ensureArchivexusStyles()` shared-style extraction *and* node-type graph colouring moved to **ADAPT-013** (item 35) — colouring is a graph concern and the sidebar no longer draws a graph. **Live-verified on v14.367 (GM, 2026-09-08):** navigator renders (Character 41 / Lore 61, "102 nodes"), correct group order, live search filters + keeps input focus, row click opens the popout re-rooted on that Node, "Whole graph" → 102 nodes, no console errors. Bonus first live check: migration 2→3 + "Seeded 17 default Relationship Definitions" ran clean on the real OPFS DB (RelationshipDefinition persistence's first live verification too). Not merged — Alberto merges.
31. ~~**VIEW-001g — first-run guidance**~~ — done (software-developer, issue #64, 2026-09-08, on `feat/view-001g-first-run-guidance`, not merged). `src/adapters/foundry/first-run-guidance.ts` (new): `GUIDANCE_STEPS` (one source of truth for the 3 steps — type Nodes → "New Relationship…" → open the graph — shared by the dialog and the panel), `buildWelcomeDialogContent` / `buildGuidancePanelHTML` (pure), `registerOnboardingSetting` (a world-scoped `config: false` boolean `onboardingDismissed` flag — **first `game.settings` use in the codebase**), `maybeShowWelcomeDialog` (GM-only, once-per-world `DialogV2.wait` with "Show me the Codex" / "Dismiss" — **any** close = dismissed, never nags; only the primary choice also switches to the Codex tab). `codex-sidebar-tab.ts` renders the inline guidance panel (GM-only) into a new `data-role="guidance-mount"`: expanded while `storage.listRelationships()` is empty (the navigator's proper empty state), collapsed to a "▸ Getting started" toggle once ≥1 Relationship exists, re-openable via the toggle or a new "Getting started" toolbar button. `module-entry.ts` registers the setting at `init`, calls `maybeShowWelcomeDialog` at `ready` after the backfill (best-effort `activateCodexSidebarTab` — `ui.sidebar.changeTab`/`activateTab` for the v13 rename). `README.md` gains a "Getting started (in Foundry)" section. 13 new unit tests (436 total); `tsc`/`eslint`/`vitest`/`build:foundry-module` all clean. **Out of scope** (per ticket): coach-marks / tour library, progress tracking beyond the flag, auto-typing Nodes. **Not yet live-verified:** the real `DialogV2.wait` render, the `game.settings` round-trip on a running world, and the `ui.sidebar` tab switch. Not merged — Alberto merges.
32. ~~ARCH-002 — relationship-surface inventory~~ — resolved inline 2026-09-08 (not a separate pass), recorded as **ADR-0014 Amendment 2**: a UI-surface "where each thing lives" table + 4 decisions (ADAPT-011 retired unbuilt → folded into the Console; per-sheet ADAPT-012 list stays, button renamed "Connections"; canonical create = the ADAPT-007 window; shared CSS = one injected `<style>` module). Provisional — expected to shift on usability testing. Unblocks VIEW-001i (#67, no longer `blocked`).
33. **ADAPT-014 — Relationship Definition editor** (issue #66). Small `ApplicationV2` (ADAPT-012 mould): list / **add** / edit / delete Relationship Definitions. This is what Alberto means by "editing" — *"adding new relation types"*. Launched from a visible entry point (Codex toolbar or the console). Closes item 29's "review the 17 defaults" flag; unblocks VIEW-001e's cluster category labels. Out of scope: Definition version migration.
34. **VIEW-001i — Relationship Console** (issue #67 — no longer blocked; ARCH-002 resolved). Singleton `ApplicationV2` launched from a **visible Codex sidebar toolbar button**. World-wide list / search / filter (by category, definition, "involves node X", dangling-only) / create (passthrough to the ADAPT-007 window) / delete (reuse the ADAPT-013 confirm). Subsumes ADAPT-011 (its "one node's connections" becomes the console's filtered-to-a-node mode). **No relationship-instance editing in v1** (swap-direction / change-definition deferred — ADR-0013 already scoped instance-editing out; Alberto's editing need is type-authoring = ADAPT-014).
35. **ADAPT-013 — shared style baseline + rough-edge fixes** (issue #68). One ticket, **last** — after VIEW-001c–f stop moving the surfaces. Defect-driven, not a reskin: the `ensureArchivexusStyles()` module + a ~10-property token layer over Foundry's CSS variables + base list-row / empty-loading-error / segmented-control components; fix the named rough edges (graph label overlap, the plain popout, cross-surface inconsistency); **colour the popout's graph nodes by `node.type`** (ADR-0014 point 3, data already exists — moved here from VIEW-001c since the sidebar no longer renders a graph). Not: theming, a UI framework, a preprocessor, global selectors.

_Remaining from the ADR-0014 Amendment split, unchanged: **VIEW-001d** (favourites — `game.user` flags, pinned navigator group; issue #69), **VIEW-001e** ("Everything connected" collapsed clusters — needs ADAPT-014 + real authored Relationships; issue #70), **VIEW-001f** ("Curated by me" + saved layout — needs CORE-006/done + VIEW-001e; issue #71)._

## Sensitive areas — don't touch or decide without asking first

- The Core's platform independence (`01_ARCHITECTURE.md`'s "Domain Ownership" section): don't let Foundry-specific concepts leak into the Core without an explicit ADR.
- Anything that would require an internal-ID migration for elements already keyed by Foundry UUID (see `ADR-0001-use-foundry-UUID.md`).

## Related

- Vision: `00_VISION.md`
- Architecture: `01_ARCHITECTURE.md`
- Ubiquitous language: `02_LANGUAGE.md`
- Domain model: `03_DOMAIN_MODEL.md`
- Decisions: `decisions/`
- Session log: `SESSION_LOG.md`
